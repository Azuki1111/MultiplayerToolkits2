# 前端设计计划

融合联机工具箱和MPH的功能，并加以优化

## Options修改
修改`Options.xml`的玩家本地名称字符长度到64

游戏本体文件地址：`D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\Options.xml`

## Config修改
- 灾害强度允许改成0 参考`D:\Game\Steam\steamapps\workshop\content\289070\3693899014\DD\DD_Config.sql`
- 基础设置更改 `D:\Game\Steam\steamapps\workshop\content\289070\3693899014\Config\GameConfig_BASE.xml`

## StagingRoom优化
核心文件`StagingRoom.lua`从游戏本体中复制出来然后以此为基础修改（本 mod 内文件位于 `FrontEnd/UI/StagingRoom/`）
1. `D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\FrontEnd\Multiplayer\StagingRoom.xml`
2. `D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\FrontEnd\Multiplayer\StagingRoom.lua`

添加和优化内容：
- 基础：
	- 房间20人数量上限修改
	- 快捷打开/关闭AI【含改版：房间「添加玩家」槽位按钮点击改为直接添加 AI 玩家（原为设为空缺槽位 SS_OPEN，现 SS_COMPUTER + SetMajorCiv，对齐 OnSlotType AI 分支）】
	- 快捷分队按钮（联机工具箱）
	- 房主权限提升，允许修改其他玩家的队伍和所选领袖（参考`D:\文档\My Games\Sid Meier's Civilization VI\Mods\乔尔定制mod\UI\StagingRoom.lua`）
	- 更新公告界面
	- 广告界面（放到左下角，并参考游戏原版主界面的滚动广告）要求使用轻松扩展的框架
	- 网络连接显示优化。修改引用文件`D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\Scripts\NetConnectionIconLogic.lua`（实现已内联进`FrontEnd/UI/StagingRoom/StagingRoom.lua`条目3.7分区，原同名覆盖文件已移除）
	- 移除玩家按钮改版【已实现为条目3.8：玩家条目行右侧 X 移除按钮与列表头「移除玩家」列移除，功能改入槽位类型下拉框选项——g_slotTypeData 新增 kickOption 哨兵项（slotStatus=-2），PopulateSlotTypePulldown 按 IsPlayerKickable 判定显隐（仅房主可见、非自己、非热座），OnSlotType 命中转 OnKickButton 走原确认弹窗流程；观察者槽位经 AlternateSlotTypePulldown 保留踢出能力；文本复用原版 LOC_MP_KICK_PLAYER】
- 进阶
	- 多人游戏mod版本校验（分析MPH，并优化）
	- 准备房间非官方模组清单（显示订阅状态 / 点击跳工坊）
	- BP面板（参考MPH但是完全重置设计）
	- 黑名单管理器与玩家标记（联机工具箱）优化框架【已实现为条目4.4：纯本地玩家标记管理面板——好友/一般/黑名单标记+记事本，数据走条目4.3 存储管线（ModGroup 组名 [MPT_DS][MPT_PlayerInfo][Players]）；python 辅助脚本留待后续】
	- 图标查看器【已实现为条目4.5：移植 Easy Icon Viewer（工坊 3173843667）至准备房间 BottomLeftButtonStack——数据表 MPT_IconCollection 顶层预加载纯数据，首开面板一次性同步构建全部实例（同原作加载方式，顺带测宽供尺寸排序），搜索过滤 + 按尺寸排序开关（文本在左圆钮在右），点击图标复制剪贴板，横向滚动同原作】
	- 贴图查看器【已实现为条目4.6：移植 TextureViewer（本作者旧作 Mods\Texture查看器）至准备房间 BottomLeftButtonStack——数据表 MPT_TextureCollection（游戏本体+全部DLC UI 贴图 5017 行）顶层预加载纯数据（正常序+来源分组排序副本一次算好），首开面板懒构建+实例池复用，搜索过滤 + 按来源包分组开关，悬停格子自定义 Tooltip 预览（真实比例/像素尺寸/贴图名/来源包，前端 ToolTipType 有 1.67 先例），点击复制贴图名，横向滚动同条目4.5；后与 4.5 融合为单面板双页签（页签仿 ClimateScreen TabLedge2/TabButton，根容器沿用 IconViewerPanel，左下角单入口按钮「查看器」打开并回到上次停留页签）】
	- 进房自动更新创意工坊mod【已实现为条目4.7：移植联机工具箱1.67 BSR UpdateAllMods 并优化——加入/创建准备房间时（OnShow 新会话分支，每进房只触发一次，替代 1.67 挂 RealizeGameSetup 每次配置刷新重复触发）对本局已启用的非官方创意工坊 mod 调 Modding.UpdateSubscription 触发更新检查；O(n²)双重循环改为单次遍历建 [modId]=SubscriptionId 映射；会话幂等守卫 + nil 防御；静默执行仅 Lua.log 留痕】
	- 房间内玩家标记显示【已实现为条目4.8：移植联机工具箱1.67 玩家标记显示层（不移植管理面板，4.4 已有）——按前端配置库 TPT_PlayerData（Shared/PlayerMark/PlayerMark_Data.sql 移植全部类型有效行：Admin/Normal/Honor/Ban，去注释/过期/测试行）登记 SteamID，在 PlayerListEntry 就绪文本（尚未就绪/已就绪/已连接态）上显示标记；本地标记优先（条目4.4 存档哈希 [Id]=记录，显示 [ICON_x]+标签名、Tooltip 昵称+简要描述换行、不受隐身影响），SQL 标记含日期时效校验（Start_Date 未来/End_Date 已过不显示）与隐身判定（HiddenPlayerInfo=="T" 隐藏 Admin/Normal/Honor、Ban 始终显示），Tooltip 优先 ToolTipType（StagingRoom.xml ContextDefaults 内联移植 1.67 TPT_ToolTipType.xml：Bermuda_Triangle 拼图 + AnDe/HuaMing/QingTian_Desc 三张 DDS 图片，DDS 经 ImportFiles 入 VFS）否则 Desc/Name/Icon 兜底；数据顶层预加载构建 [SteamID]=记录 哈希表 O(1) 直查（替代 1.67 每次刷新线性遍历），4.4 面板保存后 SaveToDisk 回调刷新缓存；隐身设置按钮后续条目另加】

## InGame 移植（条目5 起）

前端条目已完成（1/2/3.x/4.x）。从条目5 开始移植联机工具箱 1.67 的 InGame 部分，每个功能在 `InGame/` 下建自包含子目录（xml + lua + 专属文本/SQL），随 1.67 的对应缩写目录逐一落地。

- 显示地图角落【已实现为条目5：移植 1.67 RMC（RevealMapCorners）——`LuaEvents.MapPinPopup_RequestMapPin` 建真实地图钉（引擎层 create-or-get）+ `UIManager:DequeuePopup` 弹掉编辑弹窗，撑开引擎小地图世界矩形使全球比例。原理实测定论：小地图矩形（`UI.GetMinimapWorldRect`）由引擎计算只认游戏世界数据（地图钉坐标纳入矩形），UI 控件（WorldAnchor 自绘透明实例，含 Box/Image/ChangeParent 到 WorldViewControls 等变体）不参与包围盒计算，自制实例方案已实测无效弃用；副作用为留下两个可见 pin（进存档/钉列表，可删除）。幂等守卫 m_anchored 防跨房间重复建 pin；经 AddUserInterfaces（Context=InGame）注册】
- 伟人名字更新【已实现为条目6：移植 1.67 GPN——魔女环境检测，仅当 LocalizedText 已含魔女改写的伟人标记（IMHOTEPI='号码菌'）时，把炼金联赛纪念伟人名字写入 zh_Hans_CN；LoadOrder 5000000 压后覆盖，非魔女环境保护原版伟人名字】
- 战败后观战按钮【已实现为条目7：移植 1.67 EGM 并修复 bug——整文件同名覆盖原版（Exp2 版）EndGameMenu.xml，底部 ButtonStack 新增「观看」按钮（原版文本 LOC_END_GAME_MENU_OBSERVE）；逻辑经原版 include("EndGameMenu_", true) 通配符注入 EndGameMenu_MPT.lua（不需复制 1350 行主脚本）：点击置本地屏蔽标志 + 完整 Close()（DequeuePopup 移除弹窗 + ReleasePauseEvent 释放暂停 + HandlePauseGame）并替换 OnPlayerDefeat 拦截后续战败事件，修复 1.67 仅 SetHide 导致「战败画面反复弹出」（SetHide 后 IsHidden 变 true 使原版防重入守卫失效，事件再触发即重新 QueuePopup）；观察按钮仅在多人局本地玩家败亡时显隐（钩 UpdateButtonStates）】
- WorldTracker 快捷操作面板【已实现为条目8（自定义功能，非 1.67 移植项）：在游戏内左上角原版 WorldTracker.PanelStack 挂载可展开/收起快捷面板（仿作弊面板 mod：空 Context + 同名 Lua 自动执行，LoadGameViewStateDone 时 LookUpControl("/InGame/WorldTracker/PanelStack") + ChangeParent + AddChildAtIndex 索引1），标题「快捷操作」点击展开，内含「投降」「重新开始」两按钮；「投降」为团队投降投票（条目8续）；「重新开始」响应暂未配置（空回调 + TODO，后续条目接入）；原 1.67 清单 FEB 顺延为条目9】
- 团队投降投票【已实现为条目8续：WorldTracker 面板「投降」按钮发起本队投降投票——通信走 UI.RequestPlayerOperation(hostID, EXECUTE_SCRIPT, {OnStart="MPT_SurrenderVote",...})（不用 Chat 指令；Gameplay 侧 SurrenderVote_Gameplay.lua 的 GameEvents.MPT_SurrenderVote 汇总，仅房主分支处理）；票数 ≥ 半数通过 → MPT_SurrenderExecute 该队城市全部叛变自由城（CityManager.TransferCityToFreeCities，仿乔尔定制玩法mod RegicideVictory 自定义战败范式：不销毁城市/单位、不做引擎判负，只写 Game:SetProperty 标记判负）；频率限制每游戏时代每队仅可发起一次（Game:SetProperty MPT_SURRENDER_VOTE_<era>_<team>，跨端同步+持久化）；投票累计存 MPT_SURRENDER_VOTES_<team>="agree/total"，每投票者一键防重复；UI 侧每帧轮询属性刷新进度/按钮显隐（仅同队存活玩家可见），投票区展开高度动态】
- 顶部面板扩展【已实现为条目9：移植 1.67 TPE（TopPanelExt）——游戏内顶部面板追加 食物/生产力/人口/奢侈品 四个统计按钮与联动 Tooltip（食物=余粮+总食物、生产力=总生产力、人口=人口增长+总人口、奢侈品=总量/种类+自己额外可交易+队友重复，FFA 时显示所有玩家）；重写 RefreshResources 战略资源 Tooltip 追加队友战略资源清单；处理 TPT_NO_TRADING_LUXURIES/TPT_NO_TRADING_STRATEGICS/SETTINGS_DIPLOMATIC_DEAL 三个交易限制配置与队伍/FFA 检测。注册：ReplaceUIScript(LuaContext=TopPanel, LoadOrder 100000) 投递 TopPanelExt.lua（基类探测 include TopPanel_Expansion2/1/TopPanel 后覆盖 RefreshYields/LateInitialize/RefreshResources）+ ImportFiles(100010) + AddUserInterfaces(Context=InGame) 挂 TopPanelExt_TT.xml（Tooltip 类型+实例模板）+ UpdateText。优化：奢侈品类型查表 O(1)（替代 1.67 数组线性遍历）、战略资源表轻量化 {Index,Hash}、队友列表每刷新周期缓存一次（替代每资源循环内 GetAliveMajorIDs）、LocalPlayer/Diplomacy 引用提升、文本预加载缓存 + 带参数 tag 拆 PRE/SUF、取消 1.67 CanRefresh 冻结缺陷（每次悬停发最新数据）。】
- 交易限制与外交限制【已实现为条目10：移植 1.67 DDV 整个模块——①交易设置四模式参数（常规=仅禁城市/经典=禁金币+城市/独立=全禁仅放行奢侈品/自定义=8 个 TPT_NO_TRADING_* 禁止项+无代表团，ParameterDependencies 仅自定义可见）+ ②无友谊（TPT_NO_FRIENDSHIP 默认开）+ ③和解设置三模式（无法和解/允许和解/默认）；执行层：DiplomacyDealView_MPT.lua 经原版 include("DiplomacyDealView_", true) 通配注入交易界面上下文（Base:3177/Exp2:3229），按限制结果覆盖 PopulateAvailable* 八类（被禁返回 1 防无法签同盟，Favor/Gold 不隐藏容器其余隐藏）。变量名优化与 TopPanel 兼容：新增统一解析器 MPT_TradeRules.lua（MPT_ResolveTradeRules() 返回八键布尔表，预设优先于自定义布尔，语义同 1.67），替代 1.67 的 8 个松散全局 isXxxTradingAllowed，TopPanel（条目9）与 DiplomacyDealView（条目10）两上下文各自 include 共用（游戏内 include mod VFS 文件可靠，前端 include 缺陷不涉及）；同时修复条目9 移植时 isLuxuriesTradingAllowed/isStrategicsTradingAllowed 恒 true 占位未接 GameConfiguration 的问题（接线 tradeRules.Luxuries/Strategics，引用零改动）。无代表团/无友谊/和解经本 mod 首个 ActionCriteria（ConfigurationValueMatches 按 GameConfiguration 值）条件执行 SQL（删 DIPLOACTION_DIPLOMATIC_DELEGATION/DECLARE_FRIENDSHIP/PROPOSE_PEACE_DEAL 三表行 + DIPLOMACY_WAR_MIN_TURNS 改 9000 或恢复 10，LoadOrder 20001/999999）；参数定义与 DealView 导入门控 Disable_MPH（ModInUse inverse MPH 619ac86e，防 Parameters 主键冲突），文本双环境注册不门控。注册：FrontEnd UpdateDatabase×2+UpdateText，InGame ImportFiles(1010)+UpdateText+UpdateDatabase×4（criteria），Version 28】
- 游戏内玩家标记面板【已实现为条目11：移植条目4.4 PlayerMarkPanel 到游戏内（自定义功能，非 1.67 移植项）——`InGame/PlayerMark/MPT_PlayerMark.xml` 整体复制 StagingRoom.xml 条目4.4 区块零尺寸改动（1024×parent 全高面板、左右列布局、添加弹窗；确认框自研 MPT_PMConfirmPopup——引擎内置 PopupDialog 模板在 AddUserInterfaces 空 Context 不实例化报错，条目11 修复），`MPT_PlayerMark.lua` 复制 4.4 面板逻辑（存储层改为 include Shared/MPT_DataStorage.lua——游戏内 include mod VFS 文件可靠，Shared 独立文件本就是为游戏内消费方保留，不再复制第三份内联副本；Modding 组存储 API 游戏内可用·用户实证，与前端 4.4 同一份 [MPT_DS][MPT_PlayerInfo][Players] 存档双向互通）。打开方式：QuickPanel 展开区新增第三枚按钮「玩家标记」→ LuaEvents.MPT_PlayerMark_Toggle（展开高度 110→146、QuickSepBottom 108→144）；关闭走 X/点面板外（ModalBlocker）/ESC（ContextPtr:SetInputHandler，EndGameMenu 同款，添加弹窗在时先关弹窗）。与前端差异：删槽位热区联动 OnSlotNameClick、删 4.8 缓存刷新调用、删 ResetOnExit（游戏内退回主菜单即销毁 UI 状态）、Open/Close 首尾带 ContextPtr:SetHide（AddUserInterfaces 上下文默认 isHidden=true）。注册：AddUserInterfaces(900)+ImportFiles(900，含 Shared 两存储文件入 VFS)+UpdateText(100，与前端同一批 PlayerMark 文本双环境注册)，Version 29】
- 强制结束回合按钮与游戏内设置面板【已实现为条目12：移植 1.67 FEB（ForcedEndButton）+ Settings（简化版）——①FEB：ActionPanel 右下角 50×50 按钮，左键 UI.RequestAction(ACTION_ENDTURN, REASON="UserForced") 强制结束回合，右键保留 LuaEvents.ForcedEndTurn 触发点（1.67 由 NHK 热键系统消费，本 mod 未移植，预留接口）；加 m_attached 幂等守卫防 LoadScreenClose 重复挂载；显隐默认隐藏，由设置广播 TPT_Settings_Toggle("ForcedEndButton_Show") 控制。②设置面板：参数表 MPT_Settings（仅 ForcedEndButton_Show 一行，ParameterId 保留 1.67 key；表名避开 1.67 TPT_Settings 无 IF NOT EXISTS 的共存建表冲突），面板移植 1.67 TPT_SettingsPanel.xml（380×512 居中 + CheckboxInstance，删齿轮入口、底部 Header 改名 HeaderBottom）；入口为 QuickPanel 展开区新增第四枚按钮「设置」→ LuaEvents.MPT_Settings_Toggle（展开高度 146→182、QuickSepBottom 144→180）；无存档时首次自动弹出。注册：AddUserInterfaces(900)×2 + ImportFiles(900)×2 + UpdateDatabase(10) + UpdateText(100)，Version 35】【条目12修复：设置存档并入 MPT_PlayerInfo 复合组（Settings.ForcedEndButton_Show，与玩家标记 Players/隐身 HiddenSqlMark 同一 ModGroupName 单组行承载；SaveComposite 全量覆盖语义 → 前端 StagingRoom 2 处 + 条目11 2 处 + 设置面板 1 处全部写入口改为读回合并防互相覆盖）；点复选框不再立即落盘（回 1.67 仅 Confirm 保存——点击即存触发 StorageCreateCleanGroup 批量禁用/启用全部 mod 致卡顿），Version 36】【条目4.3重构：MPT_DataStorage 统一多表存储——旧 5 API（SaveData/LoadData/DeleteFile 单 key 组 + SaveComposite/LoadComposite）删除，新 3 API（LoadAll 整组读 / GetTable 单表读 / SaveTables 按表名覆盖写，内部自动读回合并、调用方零 merge）；数据格式 [MPT_DS][MPT_PlayerInfo][len]return { 表名=数据 } 单组行承载，表名 = 参数 ParameterId（ForcedEndButton_Show / HiddenSqlMark / Players），未来新功能 = 新表名；前端内联副本与 Shared/MPT_DataStorage.lua 同步重写，12 处调用迁移（前端 StagingRoom 5 + 条目11 4 + 设置面板 3）；不兼容旧存档（用户确认），Version 37】【条目12优化：设置面板独立目录 InGame/SettingsPanel/（git mv，与 ForcedEndTurn 拆分：FEB 按钮 tooltip LOC_FORCEEND_TT 留 ForcedEndTurn，设置项文本独立 SettingsPanel_zh/en.sql）；命名规范化 TPT→MPT（广播事件 LuaEvents.TPT_Settings_Toggle→MPT_Settings_Toggle、TPT_Settings_CheckBoxs→MPT_Settings_CheckBoxs、CreatCheckBoxStack→MPT_SettingsPanel_CreateCheckboxes、OnCheckBoxs→MPT_SettingsPanel_OnToggle、SetCheck→MPT_SettingsPanel_ApplyAll、storageData→MPT_SettingsPanel_Save、Initializedata→MPT_SettingsPanel_Load、OnShow/Close/SettingButton→MPT_SettingsPanel_Show/Close/OnOpen、XML ID TPTSettingsPanel→MPTSettingsPanel、文本 tag LOC_SHOW_FEB_*→LOC_MPT_SETTINGS_SHOW_FEB_*）；事件改名后与 1.67 广播互不联动（共存时各自控制各自 FEB）；历史功能 TPT 命名（DealRestriction 参数 key、TPT_PlayerData 表等）不在范围；modinfo 路径/Text action 拆分（IG_MPT_Settings_Text 新增）、Version 38】【条目12修复：删首次自动弹窗（1.67 First_Use 引导——本 mod 有 QuickPanel「设置」按钮明确入口，自动弹窗依赖本地存档判定且游戏内不可靠，此前每次进游戏都弹）；StorageCreateCleanGroup 禁用/恢复包 pcall（游戏内 InGame Context 的 DisableAllMods 流程不可用/抛错时静默退化为直接建组，修复存档写入失败根因——设置/玩家标记持久化受益；Shared+内联副本同步），Version 39】【条目12修复：设置面板开关入口独立事件名 LuaEvents.MPT_SettingsPanel_Toggle（QuickPanel「设置」按钮触发，无参）——原与参数广播 MPT_Settings_Toggle(ParameterId, Value) 共用事件名，点复选框广播时面板自身 OnOpen 监听响应导致面板立即自关闭；拆分后 MPT_Settings_Toggle 仅作参数广播（FEB 按钮消费），Version 40】

- 游戏内自动更新【已实现为条目13：移植 1.67 Update/AutoUpdate 纯逻辑部分（变量名 TPT→MPT 优化）——进游戏（空 Context 同名 Lua 自动执行，AddUserInterfaces Context=InGame，同条目5 RMC 机制）即对本局已启用的非官方创意工坊 mod 触发更新检查（复用条目4.7 优化：单次遍历建 [modId]=SubscriptionId 映射 + nil 防御 + 非官方过滤 + print 留痕，替代 1.67 O(n²) 双重循环）；并确保本 mod 已启用（EnableMods 按本 mod GUID 匹配 Handle——本地 mod 无订阅 ID，1.67 按 SubscriptionId 匹配不适用）；退出到主菜单（Events.ExitToMainMenu）时触发本 mod 工坊更新（按 MPT_MOD_ID 定位安装列表取 SubscriptionId，发布后自动生效，无订阅 ID 跳过——不依赖硬编码订阅 ID 常量）。不移植：更新内容 Tooltip 展示（Get_TPT_Update_Text/Creat_TPT_Update/OnLoadScreenClose——条目3.5 前端更新公告面板已有，本 mod 不覆盖 WorldTracker 标题控件）、固定订阅列表更新（1.67 特有配置）。注册：AddUserInterfaces(900) 仅 xml + 同名 lua 自动执行 + `<Files>` 登记，无文本（纯静默逻辑）。Version 42】
- 真实科文进度【已实现为条目14：移植 1.67 TCP（Tech And Civic Progress）——科技树/市政树/研究选择器/市政选择器显示真实研究进度：Tooltip「研究进度 x / y + 获得提升后预估 + 可凭提升完成」、可凭 boost 完成的节点/条目名追加 [icon_You]、modifier 附加科技/文化加速计入（EFFECT_ADJUST_TECHNOLOGY_BOOST / EFFECT_ADJUST_CIVIC_BOOST）、BLOCKED 状态回合数按实际产出重算。对比结论：1.67 基于 Base 整文件复制 + 函数内修改 + 尾部补抄官方 Exp1（联盟研究图标）/Exp2（IsSearchable）——本 mod 改为 include 探测官方最高版本（TechTree_Expansion2→1→Base / CivicsTree_Expansion2→Base）自动获得官方功能，零复制；共享脚本 TechAndCivicSupport.lua 被 7 个 context include 只能整文件同名覆盖（ImportFiles 100010 压过 1.67 的 2001）。优化：删 1.67 调试代码、函数 MPT 前缀、缓存变量 local 化、文本预加载缓存拆 PRE/SUF、去冗余 Boosted 字段（用原版 BoostTriggered）、「可凭 boost 完成」图标走数据层（GetResearchData/GetCivicData 直接给 kData.Name 追加图标，ResearchChooser/CivicsChooser 显示自动生效**无需替换 context**——1.67 需改 4 个文件）、boost 静态表 MPT_BoostMap 顶层一次构建 O(1) 查表（替代 1.67 刷新静态数据 PopulateItemData + OnOpen）、GetExtraBoostFromModifiers 每回合缓存。默认启用无开关（同条目9 TPE 风格）；与 1.67 共存时本 mod LoadOrder 100000 压过。注册：ReplaceUIScript(100000)×2（TechTree/CivicsTree）+ ImportFiles(100010)（TechAndCivicSupport + 2 MPT lua）+ UpdateText(100) 双语言。Version 43】
- 队友资源可见性显示【已实现为条目15：移植 1.67 STR（STR/UI/WorldViewIconsManager_STR.lua）——同队队友已通过科技/市政解锁、而本地玩家尚未解锁的资源，在地图已探索迷雾格显示迷雾样式图标（条目9 顶部面板「队友战略资源清单」的地图侧对应能力）；区域拍下盖住资源时按可见性刷新该格、总督任命（Exp1+，结社/总督解锁场景）一次性补扫、外部刷新接口 LuaEvents.MPT_WorldViewIcon_Rebuild（1.67 TPT_WorldViewIcon_Rebuild 的 MPT 命名版）。实现：ReplaceUIScript(LuaContext=WorldViewIconsManager, LoadOrder 100000) 投递 InGame/TeamVisibleResources/WorldViewIconsManager_MPT.lua（include 原版后覆盖 Initialize/GetNonEmptyAt/OnResearchCompleted/OnCivicCompleted/OnShutdown）+ ImportFiles(100010)；原版该文件仅 Base 一个版本（DLC/资料片无变体，已验证），无需版本探测（区别条目9/14）；目录名用描述性命名对齐 InGame 规范（非 1.67 缩写 STR）。优化：①观察者/热座 nil 防御（条目7 观战场景，1.67 每回合 Players[localPlayer]:GetTeam() 报错）②1.67 两个 90% 重复扫描函数合并为 MPT_UpdateTeamVisibleResources（Governor 版保持一次性自移除语义）③解锁科技/市政表 [Type]=Index O(1) 映射 + 未解锁资源轻量 {Index,Hash}（替代数组线性扫描/整行存储）④本地玩家自己已可见的资源剪除出扫描表（1.67 永久残留致空表早退失效）⑤LoadGameViewStateDone + Initialize 尾部立即同步（1.67 读档/中途加入后要等首回合结束才首次扫描）⑥OnShutdown 覆盖卸载全部监听（1.67 注册后从不清除）⑦GovernorAppointed 仅 Exp1+ 存在，挂载/卸载前 nil 守卫。无文本无 XML（图标 Tooltip 全复用原版 tag）。Version 44】
- 禁用地图钉【已实现为条目16：移植 1.67 RMP（Remove Map Pins，目录 `InGame/DisableMapPins/`，描述性命名对齐 InGame 规范）——高级选项新增金色布尔开关「禁用地图钉」（CPL_NO_PINS，默认关、开局后不可改、SortIndex 2902，ParameterId 沿用 1.67 原名与 1.67 同装时共用同一开关行，同条目10 保留参数名先例；文本 tag 同样沿用 CPL_NO_PINS_NAME/DESC，中文照抄 + 英文本 mod 补全），勾选后进局：①LoadScreenClose 隐藏左下角小地图「地图钉列表」按钮（DisableMapPins.lua，函数 MPT 前缀 + LookUpControl nil 防御）②空壳整体替换原版 MapPinListPanel.lua（多人联机卡顿元凶，MapPinListPanel_MPT.lua 经 ReplaceUIScript LoadOrder 100000 压过 1.67 同类替换的 2010 + MapPinListPanel.xml 空壳同名覆盖布局双保险）；地图钉本身（旗子渲染/放置）不受影响。三个 InGame action（ReplaceUIScript/ImportFiles/AddUserInterfaces）均由 No_Map_Pins criteria（ConfigurationValueMatches Game/CPL_NO_PINS/1，criteria id 镜像 1.67）门控——不勾选时零改动原版；参数 FE UpdateDatabase(10) 无条件注册（复选框常在）+ 文本 FE UpdateText(100) 仅前端注册（游戏内无本条目文本）。不移植：1.67 NT 模块的进局通知「地图钉已禁用」（属 NT 而非 RMP，最小范围）。Version 46】
- 建立城市免确认【已实现为条目17：移植 1.67 RCT（移除建立城市弹窗，目录 `InGame/InstantFoundCity/`，描述性命名对齐 InGame 规范）——点击「建立城市」不再弹确认弹窗（含「将移除地貌」提示），直接坐城；无条件常开同 1.67（用户确认不加开关）。机制：ReplaceUIScript 抢占 UnitPanel 上下文入口（LoadOrder 100000 压过 1.67 同类替换的 11000 + ImportFiles 100010，均无 criteria）→ 文件内手动重建原版 include 链（BSM 观战 unitpanel_spec 优先 → XP2 → XP1 → Base，include 对缺文件仅告警、命中全局 Initialize 即 break 防重复订阅；BSM 的 unitpanel_spec 优先级无法用 modinfo criteria 静态表达，运行时自适应更稳）→ 重定义全局 OnUnitActionClicked_FoundCity（原版按钮回调是匿名闭包，点击时才解析全局名，include 后重定义即生效，无需重建动作表；函数名保持原版全局名不可 MPT 化）。优化：①自建 local MPT_HexColoringWaterAvail（UILens.CreateLensLayerHash，原版引擎拼写 Availablity 照抄）修复 m_HexColoringWaterAvail 原版 UnitPanel.lua 顶部 local 跨 include 不可见致 nil——1.67 尾部 UILens.IsLayerOn(nil) 报错被引擎吞掉，坐城后「可坐城水域」高亮层不关闭、镜头不回默认 ②删 popupString 两次 Locale.Lookup 死代码（kResults 分支整体删除，参数仅兼容原版调用签名保留）③注释尸体清理函数重写，原版弹窗逻辑以修改留痕横幅保留 ④include 链注释补全 + print 加 MPT 前缀。无文本无 XML 无参数。Version 49】
- 城市横幅刷新【已实现为条目18：移植 1.67 PCF（pk-civ6 皮皮凯「城市横幅刷新修复」，目录 `InGame/CityBannerRefresh/`，描述性命名对齐 InGame 规范）——修两件事：①原版 CityBannerManager 不订阅 CityWorkerChanged（市民变动后横幅产出滞后）→ 延时去抖后调原版全局 RefreshBanner（UpdateStats+UpdateRangeStrike+UpdateName）单城刷新；②城界扩张/人口增长/地块产出变化（改良建成/被劫/修复）后市民不自动重排 → 延时去抖后下发 CityCommandTypes.MANAGE 公民指令（原版 PlotInfo 点击市民同款命令，坐标取城市中心），促使引擎把市民重新分配到最优地块。注入机制：文件名前缀 `CityBannerManager_` 被原版 CityBannerManager.lua 末尾官方预留通配 `include("CityBannerManager_", true)` 拉入同上下文（同条目7 EndGameMenu 机制），可直接调用上下文全局 RefreshBanner，modinfo 仅 ImportFiles(1010) 无需 LuaContext（1.67 同款纯 ImportFiles，其 LoadOrder 7 互不干扰；与 1.67 同装两文件都会被拉入、重复执行无害，建议二选一）。刷新机制重写（替代皮皮凯 LuaTimer，用户要求探索更高效刷新方式）：①单订阅调度器——1.67 每挂一个定时器就 Add/Remove 一条 GameCoreEventPublishComplete 订阅反复增删，本文件全局仅 1 条订阅驱动任务表（[taskID]={Deadline,Func,Values,Unpack}）先出表再执行、O(1) 增删 ②真实单调时钟 UI.GetElapsedTime（原版 PlotToolTip 5 秒超时同款，缺 API 兜底 os.clock）替代 1.67「发布周期计数」冒充秒数（事件批节奏不匀，2 周期可能 0.2s 也可能 10s）——2 秒即真 2 秒 ③全 local 零全局污染（1.67 AddTimer/CallbackDict 等 6 个全局与同上下文 CSB/CSA 补丁有冲突风险）④去重键统一 MPT_Refresh_/MPT_Rearrange_ 前缀命名空间（1.67 PlotYieldChanged 路径裸 cityID 当键可能互吞）⑤重排指令后兜底挂一次横幅刷新（1.67 仅依赖重排引发 CityWorkerChanged，引擎未发布时横幅滞留旧值；同键去抖不增调用）⑥防御补全：重排前城市 nil 检查（延时期间被毁/易主）、PlotYieldChanged 地块 nil 检查、观察者天然跳过。保留 1.67 语义：本地回合开始 1 秒冷却（官方回合切换整批刷新期间跳过）、本地回合结束清空未到期任务、重排 4 秒/产出 2 秒/刷新 2 秒去抖。无文本无 XML 无参数。Version 50】【条目18修复：MPT_GetTime 以 cfunction（`UI.GetElapsedTime or os.clock` 直接赋值）赋给 ifunction 标注变量——引擎类型检查报 Type check failed: expected 'ifunction', but got 'cfunction' 致文件 chunk 中止、通配 include 失败、全部功能未加载（横幅毫无反应根因，Lua.log 实证）；改为 Lua 闭包包装（调用时探测 API，MPT_ClockName 留痕时钟选择）+ 全关键路径调试 print（MPT_Banner: 前缀：文件加载留痕/回合冷却与清任务/本地玩家事件到达与冷却态/任务排程与执行/MANAGE 指令下发，实测确认后删）；Version 51】【条目18调优：去抖延时 1.67 的 4/2/2 周期计数在真实时钟下过慢——横幅刷新 2 秒→0.1 秒近实时（用户要求 100ms 左右；实际执行时刻=0.1 秒后首个 GameCoreEventPublishComplete tick，事件到达后本就紧跟批结束，体感即近实时）、市民重排 4/2 秒→0.5 秒统一合并窗口，延时收敛 MPT_REFRESH_DELAY/MPT_REARRANGE_DELAY 两常量便于调整；Version 52】【条目18清理：移除全部调试 print（MPT_Banner: 前缀 10 处——文件加载留痕/回合冷却与清任务/本地玩家事件到达与冷却态/任务排程与执行/MANAGE 指令下发），实测确认功能生效后删；仅为包 print 存在的 3 个 `if playerID == Game.GetLocalPlayer()` 块与仅被加载留痕 print 消费的 MPT_ClockName 一并删除，逻辑零改动；顺带入库 MPT_REARRANGE_DELAY 0.5→0.1（用户手调）；Version 54】
- 清理通知按钮【已实现为条目19：移植 1.67 NOC（目录 `InGame/NotificationClear/`，描述性命名对齐 InGame 规范）——通知栏插入复用原版 ItemInstance 通知条目模板的「垃圾桶」按钮（QueueTrash 图标、隐藏计数/翻页箭头/页点），点击一键清理全部可手动关闭的通知（NotificationManager.Find→CanUserDismiss→Dismiss，不可关闭保留）。注入：文件名前缀 `NotificationPanel_` 走原版通配 include 钩子进 NotificationPanel 上下文（同条目7/18，ImportFiles 1010 无 LuaContext）；链式覆写全局 OnDefaultAddNotification/ReleaseNotificationEntry（原版 MakeDefaultHandlers 对 handler.Add 延迟绑定，include 时重定义全局即生效；函数名保持原名；BASE_ chunk local 捕获零全局污染）。按钮位置（用户询问 Stack:SortChildren 能否置底）：无需采用——通知栈 Anchor R,B+StackGrowth Top（1 号子控件在最底部）、按钮先于首条通知实例懒创建恒为 1 号子控件、且永不被释放（原版 ClearNotifications/ReleaseNotificationEntry 只清理 m_notifications 登记条目，实例复用不影响未释放实例）→ 天然恒居最底部零代码保证；SortChildren 签名是比较函数非 index，O(n·logn) 重排+重布局对固定位置按钮纯属开销（InstanceManager.lua 官方注释并警告其破坏布局依赖样式）。优化：①精确存活集 [playerID][id]=true+计数替代 1.67 环形缓冲（100 上限回绕静默丢最老 ID/已释放 ID 永久残留重查）②显隐用自维护计数 >0（替代直读 m_genericItemIM.m_AllocatedInstances 内部字段+「>1 因按钮占位」魔数）③单清理回调仅注册 MouseInArea 左右键（删 MouseOutArea 重复注册的同点击双触发隐患）④设置广播 nil 双守卫（1.67 在任何通知出现前收到广播 NPQC_ClearButton.Top 崩脚——本 mod 面板 LoadScreenClose 即全量广播此场景必现）⑤Clear 先快照 ID 再 Dismiss（防同步释放回调遍历中改表）⑥零全局污染。开关：条目12 MPT_Settings 表加行（ParameterId 沿用 1.67 原 key NotificationPanel_QuickClear，反向语义同 1.67：勾选=禁用按钮，默认 0=显示；文本 LOC_MPT_SETTINGS_NOC_DISABLE_* zh+en，1.67 仅 zh），lua 订阅 MPT_Settings_Toggle 匿名闭包，初始状态经面板 LoadScreenClose ApplyAll 全量广播自动送达无需自读存档。tooltip 沿用 1.67 借用 tag LOC_HUD_MAP_SEARCH_CLEAR_SEARCH 零文本注册。与 1.67 同装时 NOC/NDR/本文件三个 NotificationPanel_* 通配共载（覆写链依次生效功能正常）但会双按钮，建议二选一。Version 53】【条目19修复：通知清空后垃圾桶按钮单独残留——计数只增不减五处漏减堵漏：A【主犯】原版 ClearNotifications 以 ipairs 遍历 m_IDs 而 ReleaseNotificationEntry 内部 table.remove 使后续左移（每隔一个跳过一个），NotificationRefreshRequested/换人/观战路径随即 DestroyInstances 整批销毁，被跳过 ID 永不走 Release→计数卡 >0 而 UI 已空——包裹原版 ClearNotifications（4 个整批清空路径公共出口，调用点运行时动态查找）BASE 后统一清账 B 原版 OnUnitKilledInCombat 笔误（playerId 应为 playerID）兜底清理 100% 失效、此类移除无 Dismissed 事件 C 换人/观战后旧玩家 ID 被 OnNotificationDismissed「非本地玩家」分支整体 return——新增 MPT_Reconcile 对账：逐 ID NotificationManager.Find 剪除引擎已消失通知（B/C 自愈），Add/Release 覆写尾统一调用，计数重算单一事实不再手动 ±1 D BASE 提前返回（typeName nil DataError/重复 ID return nil）时孤儿 ID——Add 登记前 GetNotificationEntry 查册，面板未收下不登记 E 口径假阳性：回合阻塞类通知（未选科技等）有册无 UI 实例（原版 754 行门槛），显隐改按「有 UI 实例资格（IsIconDisplayable 且非回合阻塞）」的存活数（=1.67 实例计数语义）。附带修复：DestroyInstances 两路径（RefreshRequested/观战）连按钮一起销毁但引用非 nil 致懒创建守卫永不触发按钮永久消失——包裹 OnNotificationRefreshRequested/OnStartObserverMode 置 nil 引用懒重建（重建时栈空仍为子控件 1 号恒居底部；换人路径不销毁实例故不置 nil 防存活按钮变孤儿重复建）；事件订阅发生在原版 LateInitialize 晚于通配注入、Add() 捕获的已是包裹版；Version 55】
- 智能回合计时器【已实现为条目20：移植 1.67 CC/Timer TimerPro + NHK/UI/TurnTime_HotKey 的热键与按钮部分（目录 `InGame/SmartTurnTimer/`，描述性命名对齐 InGame 规范；用户确认范围：聊天指令/PID平衡/宣战掉线加时要，15 秒滴答音不要，热键与加减时按钮一起移植；NHK 强制结束回合段落不移植——条目12 FEB 已承担）——房主在回合整体结束（Events.TurnEnd）时以「半数真人玩家结束回合时已耗回合时间」为反馈量做 PID 自动平衡（一阶滤波 0.5 → Kp 0.5/Ki 0.08/Kd 0.25 → 积分抗饱和+限幅 ±10 → 衰减 0.98 → 过时代前馈 GetNextEraCountdown 加 10/20 减 15 → 下限 30 秒取整）改写官方 TURN_TIMER_TIME 参数并 Network.BroadcastGameConfig 广播全房；宣战 +20s（大文明间）/剩余<10s 对城邦 +8s（防卡秒顶城邦）/掉线 +30s 自动加时；议会投票阶段全员开新回合后固定 120s；首回合多人强制 TURNTIMER_STANDARD 并以 30s 起步；聊天指令 p+（+20s 剩余<8s 改 +24，每回合一次）/p+++（本回合无计时，下回合恢复）/p--（下回合 -15s）仅房主监听公共频道；聊天框旁 P++/P-- 按钮（所有玩家，点击发聊天指令，房主由自己监听代执行）+ [ ] 加减时热键（GameInfo.InputActions+InputActionDefaultGestures 注册，]/[ 键位，CategoryId=ONLINE/ContextId=World 沿用 1.67；房主直接改配置广播，非房主代发 p++/p--）。加载：空 Context 经 AddUserInterfaces(900, Context=InGame)+ImportFiles(900) 同名配对（同条目12/13 机制），1.67 的 TimerPro/TurnTime_HotKey 两 Context 合并单文件；按钮 ChangeParent 到 /InGame/WorldTracker/ChatPanelContainer 在 LoadScreenClose 后执行（1.67 同款时序，防 WorldTracker 未加载 LookUpControl nil）；热键 InputActions 前后端各注册一次（FE 主菜单按键绑定界面显示名称/描述）。开关：条目12 MPT_Settings 表加行（ParameterId 沿用 1.67 原 key TOOLS_COMMAND，默认 1=开启 1.67 同款；文本 LOC_MPT_SETTINGS_TIMER_ENABLE_* zh+en），lua 文件加载即订阅 MPT_Settings_Toggle（早于面板 ApplyAll 广播必达），语义差异：1.67 为全局 Game 参数广播全房 → 本 mod 每玩家本地开关（房主控自动平衡，非房主各控自己按钮显隐/热键代发），1.67 房主热键不受开关限制 → 本 mod 统一由本机开关门控。优化：①零全局污染（1.67 十余个全局函数/变量全 local + MPT_Timer_ 前缀）②魔法数 2133509568 → DB.MakeHash("TURNTIMER_NONE")（原版 ActionPanel 同款 Hash 参数比较）③四处重复真人全遍历合并单一 CountHumans（一次遍历返回总数+已结束数；首回合初始化人数判定也复用，1.67 该处未排除观察者已修正）④PID 控制量只算一次复用+手动修正量懒计算⑤nil/类型防御（TURN_TIMER_TIME 非 number 兜底 60/PlayerConfigurations/Players/Game.GetEras 守卫/聊天文本 type 守卫——1.67 直读 nil 参与算术有崩脚隐患）⑥冷却 os.time 墙钟 → UI.GetElapsedTime 单调真实时钟闭包包装（条目18修复同款）⑦IsAnyMultiplayer 联机门控（1.67 单人局也空跑平衡并广播）⑧TurnTimerUpdated 双订阅（记录+15s 音效）单订阅仅记录⑨按钮显隐 Events.GameConfigChanged → MPT_Settings_Toggle，XML 默认 Hidden 防闪现⑩修正 1.67 减时按钮 tooltip「下回合减少20秒」与实际 -15s 不符的文本 bug。与 1.67 同装注意：双方计时器都回合末改写 TURN_TIMER_TIME 广播互相拉扯致振荡（用户确认不管控），勿同开。无 AddGameplayScripts 纯 UI 侧。Version 56】【条目20扩展：新增四模式工作模式 Game 配置参数 MPT_TIMER_MODE（`Config_SmartTimer.xml`，FE UpdateDatabase(10)——Configuration 参数须前端注册、游戏开局建库继承，条目16 CPL_NO_PINS 同款，IG 重复注册主键冲突），GameOptions 组下拉四选一（SortIndex=21 紧随原版回合计时器类型 20，ChangeableAfterGameStart=1 房主可开局后暂停菜单切模式广播全房，DomainValues 表结构经原版 SetupParameters.sql 核实 (Domain,Value,Name,Description,SortIndex)、无需 Domains 行）：OFF 关闭所有计时器（mod 完全不介入）/ BASIC 基础只包含聊天指令（p+ 立即加时、p+++ 本回合无计时下回合恢复、p-- 下回合直减 15 秒下限 30 秒——回合末最小落实逻辑，否则 p+++ 在无平衡逻辑的基础模式会永久关掉计时器）/ SMART 智能计时器（初版完整功能：PID+聊天+宣战掉线+投票 120s+过时代前馈）/ FIXED 固定计时器（纯回合阶梯曲线：初始 30 秒、30T 起 80s、50T 起 120s、70T 起 180s 后保持；不含聊天指令与任何自动调节；开局 LateInitialize 即强制 STANDARD+首回合 30 秒使「初始 30 秒」从第 1 回合生效，回合末按下一回合 CurrentTurn+1 查 MPT_FixedTiers 阶梯表）。默认 SMART（延续默认开决策）；初版 MPT_Settings 布尔行 TOOLS_COMMAND 与 SettingsPanel 文本回退删除（布尔无法表达四模式，两套开关并存会互相打架），语义差异第 1 条改回：1.67 布尔 TOOLS_COMMAND → 本 mod 四模式 Game 配置参数，全房统一跟随房主配置（回归 1.67 全局语义）；lua 删 MPT_TimerEnabled 与 MPT_Settings_Toggle 订阅 → MPT_Timer_GetMode()（type 守卫参数异常兜底 OFF fail-safe）+ MPT_Timer_IsCommandMode（BASIC|SMART 供聊天/按钮/热键门控）+ Events.TurnEnd 入口按模式分发（FIXED→固定曲线/BASIC→指令落实/SMART→PID/OFF→仅清状态），宣战/掉线/投票/半数采样门控改 ==SMART，按钮显隐恢复 Events.GameConfigChanged 随配置广播刷新；模式文本（参数 Name/Desc+四值 Name/Desc 共 10 tag×2 语言）入 SmartTurnTimer 双语 SQL。Version 61】
- 商路界面增强【已实现为条目22：移植 1.67 BTS/ 目录（BTS = Better Trade Screen Lite，Astog 原作 + 1.67 TeamPVPTools/DeepLogic 魔改，目录 `InGame/BetterTradeScreen/`，描述性命名对齐 InGame 规范；用户需求：移植并优化，解决「商路目标面板收益没有及时刷新」）——①商路总览侧边面板（原版 LaunchBar 按钮打开）：三页签（我的商路/通往我城市的商路/可用商路）+ 按出发/目的城市分组与组内展开收起 + 七级排序栏（左键插入右键删除 Shift 多级）+ 按文明/城邦/贸易任务筛选 + 每行真实收益双列 + 在途商路精确剩余回合（Tracker 本地账本按回合递减、读档经 PlayerConfigurations 持久化重建）+ 旅游业绩/外交可见性/贸易站标记 + 空闲商人「选择商路」按钮与商人传送；②商路目的地选择面板：候选目的地列表（真实收益/回合数/贸易站/城邦任务图标）+ 七级排序栏 + 筛选 + 商路透镜（全部候选路径/选中置顶）+ 上次商路自动预选；③商人传送面板（TELEPORT_TO_CITY）：横幅着色 + 城市字母序；④TradeSupport.lua 共享库（真实收益缓存体系 + Tracker + 排序打分），被总览/选择器两上下文按名 include。
  实现：原版四文件（TradeOverview/TradeRouteChooser/TradeOriginChooser/TradeSupport）均 Base 单版本无 DLC 变体（已验证）→ 整文件覆盖；三个 UI 上下文 ReplaceUIScript 100000 + TradeSupport.lua 纯 ImportFiles 遮蔽（文件名必须保持原名）+ 三 XML 同名覆盖布局，统一 ImportFiles 100010；与 1.67 同装压制其 BTS_IMPORT_FILES(11011) 本 mod 生效（文件头横幅留痕）。
  【核心修复·收益不及时刷新】1.67 根因：TradeSupport 收益缓存每回合只建一次（CacheRoutesInfo 的 m_Cache.TurnBuilt ≥ 当前回合闸），联机同时回合下开面板前发生的政策/建筑/贸易站/宣战议和变化全部显示旧值到下回合；且选择器重建触发面窄（仅换商人/跨回合/地块首次揭示）、政策变更只 Refresh 不清缓存。修复：两面板 Open() 先 CacheEmpty()（打开必新算）；OnPolicyChanged/Obsoleted 补 CacheEmpty；新增失效事件订阅（事件名均经原版 UI 使用验证）——yield 类 BuildingChanged/DistrictAddedToMap/TradeRouteAddedToMap（他方商路抵达建贸易站）清缓存+打开时 Refresh（各页签读缓存经 CacheTouchRoute 按需重算），validity 类 DiplomacyDeclareWar/DiplomacyMakePeace/TradeRouteCapacityChanged/CityAddedToMap 额外强制重建候选表（Chooser 置 m_TurnBuiltRouteTable=-1 命中重建条件、Overview 置 m_HasBuiltTradeRouteTable=false）；缓存本体保留（总览单次刷新数百行×12+ 引擎调用的性能护栏）。附带修复：①选择器 Open() 恢复上次商路引用未定义全局 DestinationCityID（应为 lastRoute.DestinationCityID，1.67 预选实际从未生效）②DeepLogic 可见性补丁 m_PlotRevealed 永不清空（探索-迷雾-再探索同格永不触发）→ 回合末清空 + 面板打开时立即 Refresh ③单数产量函数非缓存分支未定义标识符（kYieldMultipliers/pDestinationCity/kRouteInfo，1.67 中不可达未爆）④Tracker 持久化 key 笔误 Rotues→Routes（1.67 旧存档键不读取，读档按实际在途商路重建仅丢精确剩余回合计数）。
  原版 2.0 回迁（BTS 基于旧版原版）：弹窗互斥三事件 TradeRouteChooser_CloseIfPopups/ReOpen/WorldInput_MakeTradeRouteDestination + OnWorldInputMakeTradeRoute + CheckNeedsToOpen 的 UI.IsInGame() 守卫 + Open() 尾 LaunchBar_CheckPopupsOpen()（与条目8 WorldTracker 弹窗/条目12 设置面板共存）。
  裁剪（1.67 行为为准，用户未另行裁决按推荐执行）：商人自动化整体剔除（1.67 v1.34 已禁用 UI——RepeatRoute/FromTopSortEntry 复选框、AutomateTrader/FindTopRoute/RenewTradeRoutes/IsTraderAutomated 等底层与 TradeSupportAutomater_* 及 BTS_TraderAutomatedSettings 持久化、总览取消自动化按钮、XML 控件全不携带）；目的地列表对方收益列不恢复（1.67 v1.33 裁剪，顶部选中详情仍显示对方收益）。
  设置：4 个子选项收编条目12 MPT_Settings 表（ParameterId 沿用 1.67 原 key BTS_ApproximateTraderPath=0/BTS_ShowSortPriorities=0/BTS_ShowAllRoutePaths=1/BTS_ShowTraderPathOnSelection=1），MPT_Settings_Toggle 广播订阅（文件级自注册，初值=默认，ApplyAll 必达）取代 GameConfiguration BTS_* 读取与 BTS_SettingsUpdate 事件；1.67 独立 BTS 设置面板不移植（连带规避其 ContextPtr:SetInputHandler(nil 未定义 OnInputHandler) 隐患），与 1.67 同装设置互不相通（UI 文件本 mod 压制生效）。零散：1.67 遗留 dbug_print=true 与四处无条件 print 关闭。
  注册：modinfo 3×ReplaceUIScript(100000)+ImportFiles(100010)+UpdateText IG(100)（仅游戏内消费，同条目21 仅 IG 先例），Files 登记 9 文件。Version 62】
- 政策卡收益显示【已实现为条目21：移植 1.67 BRS/ 内打包的 Extended Policy Cards（EPC by Aristos；工坊 2266952591 即 EPC 本体，仅 GovernmentScreen.lua/.xml 两文件，描述自认 Depends on Better Report Screen——依赖实为 Infixo BRS 的 RMA 引擎 ExposedMembers.RMA.CalculateModifierEffect，工坊 1312585482 即 BRS v7.0 本体；1.67 v1.34 起「兼并政策卡mod」把 EPC 的 GovernmentScreen 打包进 BRS/ 目录。用户要求仅移植 EPC 部分）——政府/政策卡界面每张政策卡底部黑底半透明收益条显示真实产出 + tooltip 追加完整效果文本。范围裁决（用户问「哪些代码多余」）：1.67 BRS/ 25 文件中 21 个属 BRS 报告界面本体与配套，EPC+RMA 对其零引用（逐一 grep/diff 验证）全部剔除——reportscreen.lua/.xml + 9 个 BRSPage_*.lua（报告界面主框架与 9 页）、RLL 两文件（报告页按钮注入器）、BetterReportScreen_Database.sql（BRS 版本参数+RLLReports 表）、Config 两文件（F8 ToggleReports 热键）、Icons/FontIconsRMI 两文件（15 图标在 RMA/GovernmentScreen 中 0 引用，仅未移植报告页消费）、Text 三文件（GovernmentScreen 40 个 LOC 全为原版自带，RMA 仅 2 个原版通用 tag）；另记录 1.67 modinfo 注册 9 个不存在语言文件的缺陷。实现（目录 InGame/ExtendedPolicyCards/）：①数据层 MPT_RealModifierAnalysis.lua——RMA v7.0 引擎 ~2900 行整携（MPT_ 改名防 1.67 同装 VFS 撞名），空 Context（MPT_RealModifierAnalysis.xml）+ 同名 lua 自动执行（同条目5/12/13 机制，AddUserInterfaces 900），ExposedMembers.RMA 导出 6 函数，RF/GS 运行时探测/事件订阅/缓存零改动；唯一 BRS 硬依赖第 1 行 print 读 GlobalParameters.BRS_VERSION_*（BRS 数据库 SQL 写入、本 mod 不移植）nil 连接崩 chunk（条目18 cfunction 同类坑），删除即完全独立；②表现层 GovernmentScreen_MPT.lua——三方 diff 验证 = 当前 2.0 原版 + ARISTOS 两块补丁无版本漂移（原版无通配 include 钩子、DLC 仅小 overlay 无完整变体，只能整文件覆盖），ReplaceUIScript 100000 + ImportFiles 100010 压过 1.67 EPC_IMPORT_FILES 12001（条目15/17 模式）+ GovernmentScreen.xml 同名覆盖布局（ARISTOS EffectContainer/Effect 控件，同条目16 MapPinListPanel.xml 先例）；isBRSActive 保持 1.67 硬编码 true（EPC 独立版为 Modding.IsModActive 探测 BRS GUID，本 mod RMA 内置必在），调用点加 RMA.CalculateModifierEffect nil 防御（1.67 无防御，RMA 缺席崩 RealizePolicyCard）。无条件常开同 1.67（先例条目14/15/17，无开关无文本无图标无参数）。已知限制：RMA 输出含少量硬编码英文（Unknown effect 等）保持原样。Version 57】【条目21优化：政策卡收益类型扩展 31→89 全覆盖——经 Cache/DebugGameplay.sqlite 逐一枚举，政策卡（含 attach 一层）实际使用 90 种 EffectType 而引擎仅处理 31 种，其余一律红字 "Unknown effect"（12 张伟人点卡、13 张时代兵种生产卡、军事/外交/间谍/城邦交易/Favor 卡全盲）。新增 MPT 文本行通道（最低侵入 2 hook + 1 分区）：①MPT_LineHandlers 表 58 种 EffectType 的静态参数驱动短文本行（产量图标/资源图标 ICON_RESOURCE_*/GPP 图标 ICON_GREAT_PERSON_CLASS_*/[ICON_Favor][ICON_POWER][ICON_STRENGTH] 等，经 Icons_*.xml 核实），对象名/兵种类(PROMOTION_CLASS_*)/时代/能力走原版 LOC tag 自动本地化，自造短语注册功能文本 SQL（ExtendedPolicyCards_zh/en.sql，61 tag×2 语言；含 9 个官方 UnitAbilities.Name 为 NULL 的隐藏能力中文名，三级回退 官方Name→自有tag→ID原文）②hook① CalculateModifierEffect：per-modifier（direct+attached 各一次）查 MPT_GetModifierLine（按 ModifierId 缓存零陈旧风险）收集成行，产量串后 [NEWLINE] 拼接（卡面 Effect 为 auto 高多行 Label 自然分行）③hook② DecodeModifier：命中 MPT 类型的 modifier 跳过原 ApplyEffectAndCalculateImpact（返回空表）不再误标红字 Unknown。顺带性能优化：CalculateModifierEffect 的 GameInfo.XxxModifiers() 全表扫描（政府界面每次打开 59 卡×全表）改 (表,字段,类型) 懒索引 MPT_GetObjectModifierIds（ModifierId/ModifierID 双拼写兼容随之移入）。核实修正：战争疲劳 EffectType 实名 EFFECT_ADJUST_WAR_WEARINESS（4 政策 modifier 方向 -100/-25 减 +20 加）、GPP 参数实值 GREAT_PERSON_CLASS_*、兵种参数 PROMOTION_CLASS_*、旅游折扣参数名 Modifier、资源图标 ICON_RESOURCE_* 非 RES_*。原 31 种处理逻辑与产量计算零改动（保真）。Version 58】【条目21修复：MPT_RealModifierAnalysis.lua:1836 语法错误 <name> expected near 'function'——类型标注位写保留字 function（local pHandler:function）被引擎 Lua 方言解析期拒绝致 RMA 上下文整个未加载（条目18 cfunction/ifunction 运行期类型检查的解析期同族坑，块配平类自检查不出）；去标注修复（pHandler 仅被调用无类型检查需求），连带去除泛型 for 循环变量标注（for _,x:string in ipairs 原版 UI 零先例，数值 for 标注有先例）。两条语法坑已沉淀 civ6 技能库失败案例库第七批 47/48。Version 59】【条目21修复：GovernmentScreen_MPT.lua:1514 EnsureRowContentsFit attempt to index a nil value（Lua.log 实证发生在 OnLoadGameViewStateDone 后界面未打开时）——卡实例数组 m_ActiveCardInstanceArray 在动画帧回调 EnsureRowContentsFit（SwitchTab 的 RowAnim 每帧驱动）触发时存在槽位空洞（实例填充循环若中断即半空）。政策卡链路内三层防御（不动原版布局逻辑）：①RealizePolicyCard 的 RMA 调用 pcall 化——内部异常不再中断实例填充循环，异常打 MPT_GovScreen: 留痕回退原版行为 ②EffectContainer/Effect nil 防御——控件来自 GovernmentScreen.xml 的 ARISTOS 补丁块，XML 同名覆盖未生效/被改时回退原版行为不崩 ③EnsureRowContentsFit/OverlapProperly 实例缺失防御 + MPT_GovScreen: 定位 print（row/slot/arrayCount，实测确认后删）+ OnRowAnimCallback 开头 ContextPtr:IsHidden 守卫（隐藏态动画回调直接跳过）。Version 60】【条目21用户裁决：显示类型 58→20 收敛（新增 MPT_KnownEffects 静默机制）——用户逐条审核后仅保留「明确的资源数量加成/产出加成(金/信/科/文/产/食/外交支持)/伟人点数/生产对象的生产力加成」20 种：时代/单位/建筑/区域/项目/全项目/太空/全单位/时代奇观 9 种对象生产力、战略资源积累、对城邦商路平产、外交支持三型（每回合/每座建筑/决议返还）、每城免费资源(attach)、伟人点数、影响力点、联盟点、建造者次数（后三者供用户进一步裁决）；其余 38 种登记 MPT_KnownEffects 静默化（费用折扣/旅游/击杀战利/劫掠百分比/使者倍率/审判次数/移动/经验/战力/间谍/厌战/电力/开关定性类）——hook② 同样跳过原链不红字，MPT_GetModifierLine 改 type(pHandler)=="function" 判定防表值 false 被调用。显示行一律去除回合描述（/每回合 删除，MPT_PerTurn 废弃）。图标 token 按本 mod IconViewer_Data.sql（游戏 XML 全量收集 4975 个真实图标文本）核对：[ICON_Favor]→[ICON_FAVOR]（表内原始形式），[ICON_RESOURCE_*/GREAT_PERSON_CLASS_*/BUILD_CHARGES] 经核实存在；免费电力按用户要求静默。功能文本 SQL 收敛为 12 短语×2 语言。自检升级：luaparser 剥标注解析通过（条目22 同款方法）。Version 63】【条目21用户裁决·实际计算：显示从静态参数升级为实际数值——①生产族 9 种走新 MPT_ImpactHandlers 产量通道（ApplyEffectAndCalculateImpact 顶部 hook，真实产量数字 +X [生产图标]，DecodeModifier 城市集合自动跨城求和）：城市当前生产对象经 BuildQueue hash 反查四表（GameInfo 按 hash 索引，同 CitySupport 203-206 行）匹配判定（含 UnitReplaces/BuildingReplaces/DistrictReplaces 文明替代、PromotionClass+PrereqTech→EraType 时代、Projects.SpaceRace、奇观 PrereqTech/Civic 时代区间）→ 城市生产力×Amount% ②资源积累=Amount×玩家提取中地块数（Map 全图遍历+IsResourceExtractableAt 引擎判定含被区域/奇观覆盖，MPT_ExtractCache 缓存）③每城免费资源=Amount×城市数 ④每座建筑支持=Favor×建筑数 ⑤对城邦商路=Amount×对城邦商路条数 ⑥伟人点=Amount×至下一位招募回合数（上限剩余缺口；池经 Game.GetGreatPeople():GetTimeline() 取未招募最低 Cost，点数接口 GetPointsTotal/GetPointsPerTurn 同原版 GreatPeoplePopup 800-801 行）——五类登记 MPT_DynamicHandlers 实时计算不进缓存，RefreshBaseData 时失效行/提取缓存；无收益时整行不显示（如无提取地块/无对城邦商路）。顺带修复：MPT_ImpactProduction/CityProductionImpact 的 pMatch:function 保留字标注复发（luaparser 自检当场抓出，条目21修复沉淀方法见效）。Version 64】

# 流程与规范
- 按顺序依次实现，每完成一条内容立即停止，等待用户确认功能是否实现

例如完成"房间20人数量上限修改"时立即停止并询问用户是否已经实现

- 对实现功能有疑惑时使用技能/skill:deep-probe 来追问用户
- 代码修改留痕迹与注释，例如
```Lua
-- ============================================================================
-- 修改房间最大人数 12 -> 20
-- g_currentMaxPlayers = math.min(MapConfiguration.GetMaxMajorPlayers(), 12);
g_currentMaxPlayers = math.min(MapConfiguration.GetMaxMajorPlayers(), 20);
-- ----------------------------------------------------------------------------
```

```Xml
<!-- ============================================================================ -->
<!-- 修改本局玩家名称上限 22 -> 128 -->
<!-- 
<EditBox ID="LANPlayerNameEdit" Anchor="L,C" Offset="0,0" Size="parent,23" EditMode="1" Style="FontNormal14" ColorSet="BodyTextCool" FontStyle="stroke" MaxLength="22"/> 
-->

<EditBox ID="LANPlayerNameEdit" Anchor="L,C" Offset="0,0" Size="parent,23" EditMode="1" Style="FontNormal14" ColorSet="BodyTextCool" FontStyle="stroke" MaxLength="128"/>
<!-- ---------------------------------------------------------------------------- -->
```

- 对于新的功能性函数需要添加注释和用法说明。
- 所有文本更改（本地化）一律采用 SQL 格式（INSERT OR REPLACE INTO LocalizedText），通用文本统一存放于 FrontEnd/Text/ 文件夹，每个语言一个文件，同语言集中，使用多行 VALUES；功能专属文本随功能文件夹存放（如更新公告 FrontEnd/Changelog/，数据表与文本同目录）
- 本 mod 新增文本在 StagingRoom.lua 中使用时，按条目分区头后集中预加载为 local XxxStr = Locale.Lookup("LOC_...") 缓存，运行时直接引用变量、不内联 Locale.Lookup；带参数文本不用 Locale.Lookup(tag, args)，改为无参数纯文本 tag + Lua .. 拼接（如 4.1 明细行拆 PREFIX/SUFFIX）。缓存块放分区头之后、本分区函数定义之前。XML String=/ToolTip= 属性与数据驱动的动态 key（row.TextTag、adEntry.ToolTipTag、Modding Name/Description 等）不预加载。
- 同一类函数放在规范的区域
- git管理，每完成一次修改任务后提交git
- 代码要可读性，易管理，命名规范，可扩展性
- 遵循游戏官方的代码风格
- 查看 /skill:civ6 这是专门用于文明6mod开发的技能
- 所有文本均使用.sql，同种语言写在同一个Values下
- modinfo规范
	- Mod id GUID 前8位为0
	- 包含<Name><Description><Teaser><Authors><SpecialThanks><AffectsSavedGames><CompatibleVersions> 并且使用本地化文本多语言，预留<LocalizedText>标签，并在最后完成mod时写入。Description中要多使用\[newline]更加美观
	- modinfo种要包含注释，简要概括该文件的修改内容和作用
	


	
	