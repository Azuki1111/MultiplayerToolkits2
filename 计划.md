# 前端设计计划

融合联机工具箱和MPH的功能，并加以优化

## Options修改
修改`Options.xml`的玩家本地名称字符长度到64

游戏本体文件地址：`D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\Options.xml`

## Config修改
- 灾害强度允许改成0 参考`D:\Game\Steam\steamapps\workshop\content\289070\3693899014\DD\DD_Config.sql`
- 基础设置更改 `D:\Game\Steam\steamapps\workshop\content\289070\3693899014\Config\GameConfig_BASE.xml`

## StagingRoom优化
核心文件`StagingRoom.lua`从游戏本体中复制出来然后以此为基础修改（本 mod 内文件位于 `FrontEnd/UI/StagingRoom/`）
1. `D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\FrontEnd\Multiplayer\StagingRoom.xml`
2. `D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\FrontEnd\Multiplayer\StagingRoom.lua`

添加和优化内容：
- 基础：
	- 房间20人数量上限修改
	- 快捷打开/关闭AI【含改版：房间「添加玩家」槽位按钮点击改为直接添加 AI 玩家（原为设为空缺槽位 SS_OPEN，现 SS_COMPUTER + SetMajorCiv，对齐 OnSlotType AI 分支）】
	- 快捷分队按钮（联机工具箱）
	- 房主权限提升，允许修改其他玩家的队伍和所选领袖（参考`D:\文档\My Games\Sid Meier's Civilization VI\Mods\乔尔定制mod\UI\StagingRoom.lua`）
	- 更新公告界面
	- 广告界面（放到左下角，并参考游戏原版主界面的滚动广告）要求使用轻松扩展的框架
	- 网络连接显示优化。修改引用文件`D:\Game\Steam\steamapps\common\Sid Meier's Civilization VI\Base\Assets\UI\Scripts\NetConnectionIconLogic.lua`（实现已内联进`FrontEnd/UI/StagingRoom/StagingRoom.lua`条目3.7分区，原同名覆盖文件已移除）
	- 移除玩家按钮改版【已实现为条目3.8：玩家条目行右侧 X 移除按钮与列表头「移除玩家」列移除，功能改入槽位类型下拉框选项——g_slotTypeData 新增 kickOption 哨兵项（slotStatus=-2），PopulateSlotTypePulldown 按 IsPlayerKickable 判定显隐（仅房主可见、非自己、非热座），OnSlotType 命中转 OnKickButton 走原确认弹窗流程；观察者槽位经 AlternateSlotTypePulldown 保留踢出能力；文本复用原版 LOC_MP_KICK_PLAYER】
- 进阶
	- 多人游戏mod版本校验（分析MPH，并优化）
	- 准备房间非官方模组清单（显示订阅状态 / 点击跳工坊）
	- BP面板（参考MPH但是完全重置设计）
	- 黑名单管理器与玩家标记（联机工具箱）优化框架【已实现为条目4.4：纯本地玩家标记管理面板——好友/一般/黑名单标记+记事本，数据走条目4.3 存储管线（ModGroup 组名 [MPT_DS][MPT_PlayerInfo][Players]）；python 辅助脚本留待后续】【条目4.9补记（游内半，此前只在代码注释未入本文档）：游戏内条目11 面板增「房间玩家」页签——MPT_PlayerMark.xml 加 Tab 台（Controls_TabLedge2_Fill+Controls_TabLedge2 双层+MarkSavedTabButton/MarkRoomTabButton，原左列内容包入 PlayerMarkSavedContent）+ MarkRoomEntryInstance 房间行实例（领袖头像45+昵称+在线状态+添加按钮/标记图标），MPT_PlayerMark.lua 加 SelectTab/RebuildRoomList（数据源=Gameplay 侧 MPT_RoomPlayers_Gameplay.lua 真人快照：IsHuman 过滤含观察者、排除自己、掉线不删=保留历史玩家，双写 Game:SetProperty 持久层+ExposedMembers.MPT_RoomPlayers 共享表，PlayerAddedToGame 追加+首回合兜底全量扫；行点击选中联动右侧编辑器，g_RoomSelectedId 金框+g_PlayerMarkRightHidden 非法ID第三态）；页签 6 个文本 tag（TAB_SAVED/TAB_ROOM/EMPTY_ROOM/CONN_ONLINE/CONN_OFFLINE/HINT_ID_UNAVAILABLE）随该条目预留进 Shared/PlayerMark 文本 SQL 双环境注册】【条目4.9前端：4.4 面板补「房间玩家」页签，自游内条目11 原样移植回前端（用户裁决「StagingRoom 不需要存储历史玩家，显示当前玩家即可」）——与游内快照版唯一差异=数据源改实时枚举：GetMultiplayerPlayerIDs 过滤 SS_TAKEN/SS_OBSERVER+IsHuman+非本机=当前在房真人，零存储零历史（掉线/退房即从列表消失；AI/开放/关闭槽位无网络ID不可标记不入列），行=领袖头像45（ICON_<GetLeaderTypeName>+LEADER_DEFAULT 兜底）+昵称+在线状态（Network.IsPlayerConnected）+未标记添加按钮（OpenAddPopup 预填）或已标记绿黄红图标（非法ID→Exclamation+HINT_ID_UNAVAILABLE tooltip）；nid 合法性复用 MPT_PlayerMark_IsValidId（与添加弹窗创建按钮同口径）；优于游内：ApplySave/CreateFromPopup/DeleteSelected 落盘成功回调补 RebuildRoomList（标记增删后房间行即时翻转，游内漏了）；刷新=Open 读盘回调重建+PlayerInfoChanged/MultiplayerPostPlayerDisconnected/GameInfoUpdated 三事件 nil 守卫订阅（仅面板开着才刷）+ResetOnExit 房间态归位回存储页；页签按钮注册进 4.4 注册区（游内错挂 LoadSettings 回调内不采纳）；零文本改动（tag 已预留）零 modinfo 结构改动，Version 89】
	- 图标查看器【已实现为条目4.5：移植 Easy Icon Viewer（工坊 3173843667）至准备房间 BottomLeftButtonStack——数据表 MPT_IconCollection 顶层预加载纯数据，首开面板一次性同步构建全部实例（同原作加载方式，顺带测宽供尺寸排序），搜索过滤 + 按尺寸排序开关（文本在左圆钮在右），点击图标复制剪贴板，横向滚动同原作】
	- 贴图查看器【已实现为条目4.6：移植 TextureViewer（本作者旧作 Mods\Texture查看器）至准备房间 BottomLeftButtonStack——数据表 MPT_TextureCollection（游戏本体+全部DLC UI 贴图 5017 行）顶层预加载纯数据（正常序+来源分组排序副本一次算好），首开面板懒构建+实例池复用，搜索过滤 + 按来源包分组开关，悬停格子自定义 Tooltip 预览（真实比例/像素尺寸/贴图名/来源包，前端 ToolTipType 有 1.67 先例），点击复制贴图名，横向滚动同条目4.5；后与 4.5 融合为单面板双页签（页签仿 ClimateScreen TabLedge2/TabButton，根容器沿用 IconViewerPanel，左下角单入口按钮「查看器」打开并回到上次停留页签）】
	- 进房自动更新创意工坊mod【已实现为条目4.7：移植联机工具箱1.67 BSR UpdateAllMods 并优化——加入/创建准备房间时（OnShow 新会话分支，每进房只触发一次，替代 1.67 挂 RealizeGameSetup 每次配置刷新重复触发）对本局已启用的非官方创意工坊 mod 调 Modding.UpdateSubscription 触发更新检查；O(n²)双重循环改为单次遍历建 [modId]=SubscriptionId 映射；会话幂等守卫 + nil 防御；静默执行仅 Lua.log 留痕】
	- 房间内玩家标记显示【已实现为条目4.8：移植联机工具箱1.67 玩家标记显示层（不移植管理面板，4.4 已有）——按前端配置库 TPT_PlayerData（Shared/PlayerMark/PlayerMark_Data.sql 移植全部类型有效行：Admin/Normal/Honor/Ban，去注释/过期/测试行）登记 SteamID，在 PlayerListEntry 就绪文本（尚未就绪/已就绪/已连接态）上显示标记；本地标记优先（条目4.4 存档哈希 [Id]=记录，显示 [ICON_x]+标签名、Tooltip 昵称+简要描述换行、不受隐身影响），SQL 标记含日期时效校验（Start_Date 未来/End_Date 已过不显示）与隐身判定（HiddenPlayerInfo=="T" 隐藏 Admin/Normal/Honor、Ban 始终显示），Tooltip 优先 ToolTipType（StagingRoom.xml ContextDefaults 内联移植 1.67 TPT_ToolTipType.xml：Bermuda_Triangle 拼图 + AnDe/HuaMing/QingTian_Desc 三张 DDS 图片，DDS 经 ImportFiles 入 VFS）否则 Desc/Name/Icon 兜底；数据顶层预加载构建 [SteamID]=记录 哈希表 O(1) 直查（替代 1.67 每次刷新线性遍历），4.4 面板保存后 SaveToDisk 回调刷新缓存；隐身设置按钮后续条目另加】

## InGame 移植（条目5 起）

前端条目已完成（1/2/3.x/4.x）。从条目5 开始移植联机工具箱 1.67 的 InGame 部分，每个功能在 `InGame/` 下建自包含子目录（xml + lua + 专属文本/SQL），随 1.67 的对应缩写目录逐一落地。

- 显示地图角落【已实现为条目5：移植 1.67 RMC（RevealMapCorners）——`LuaEvents.MapPinPopup_RequestMapPin` 建真实地图钉（引擎层 create-or-get）+ `UIManager:DequeuePopup` 弹掉编辑弹窗，撑开引擎小地图世界矩形使全球比例。原理实测定论：小地图矩形（`UI.GetMinimapWorldRect`）由引擎计算只认游戏世界数据（地图钉坐标纳入矩形），UI 控件（WorldAnchor 自绘透明实例，含 Box/Image/ChangeParent 到 WorldViewControls 等变体）不参与包围盒计算，自制实例方案已实测无效弃用；副作用为留下两个可见 pin（进存档/钉列表，可删除）。幂等守卫 m_anchored 防跨房间重复建 pin；经 AddUserInterfaces（Context=InGame）注册】
- 伟人名字更新【已实现为条目6：移植 1.67 GPN——魔女环境检测，仅当 LocalizedText 已含魔女改写的伟人标记（IMHOTEPI='号码菌'）时，把炼金联赛纪念伟人名字写入 zh_Hans_CN；LoadOrder 5000000 压后覆盖，非魔女环境保护原版伟人名字】
- 战败后观战按钮【已实现为条目7：移植 1.67 EGM 并修复 bug——整文件同名覆盖原版（Exp2 版）EndGameMenu.xml，底部 ButtonStack 新增「观看」按钮（原版文本 LOC_END_GAME_MENU_OBSERVE）；逻辑经原版 include("EndGameMenu_", true) 通配符注入 EndGameMenu_MPT.lua（不需复制 1350 行主脚本）：点击置本地屏蔽标志 + 完整 Close()（DequeuePopup 移除弹窗 + ReleasePauseEvent 释放暂停 + HandlePauseGame）并替换 OnPlayerDefeat 拦截后续战败事件，修复 1.67 仅 SetHide 导致「战败画面反复弹出」（SetHide 后 IsHidden 变 true 使原版防重入守卫失效，事件再触发即重新 QueuePopup）；观察按钮仅在多人局本地玩家败亡时显隐（钩 UpdateButtonStates）】
- WorldTracker 快捷操作面板【已实现为条目8（自定义功能，非 1.67 移植项）：在游戏内左上角原版 WorldTracker.PanelStack 挂载可展开/收起快捷面板（仿作弊面板 mod：空 Context + 同名 Lua 自动执行，LoadGameViewStateDone 时 LookUpControl("/InGame/WorldTracker/PanelStack") + ChangeParent + AddChildAtIndex 索引1），标题「快捷操作」点击展开，内含「投降」「重新开始」两按钮；「投降」为团队投降投票（条目8续）；「重新开始」响应暂未配置（空回调 + TODO，后续条目接入）；原 1.67 清单 FEB 顺延为条目9】
- 团队投降投票【已实现为条目8续：WorldTracker 面板「投降」按钮发起本队投降投票——通信走 UI.RequestPlayerOperation(hostID, EXECUTE_SCRIPT, {OnStart="MPT_SurrenderVote",...})（不用 Chat 指令；Gameplay 侧 SurrenderVote_Gameplay.lua 的 GameEvents.MPT_SurrenderVote 汇总，仅房主分支处理）；票数 ≥ 半数通过 → MPT_SurrenderExecute 该队城市全部叛变自由城（CityManager.TransferCityToFreeCities，仿乔尔定制玩法mod RegicideVictory 自定义战败范式：不销毁城市/单位、不做引擎判负，只写 Game:SetProperty 标记判负）；频率限制每游戏时代每队仅可发起一次（Game:SetProperty MPT_SURRENDER_VOTE_<era>_<team>，跨端同步+持久化）；投票累计存 MPT_SURRENDER_VOTES_<team>="agree/total"，每投票者一键防重复；UI 侧每帧轮询属性刷新进度/按钮显隐（仅同队存活玩家可见），投票区展开高度动态】
- 顶部面板扩展【已实现为条目9：移植 1.67 TPE（TopPanelExt）——游戏内顶部面板追加 食物/生产力/人口/奢侈品 四个统计按钮与联动 Tooltip（食物=余粮+总食物、生产力=总生产力、人口=人口增长+总人口、奢侈品=总量/种类+自己额外可交易+队友重复，FFA 时显示所有玩家）；重写 RefreshResources 战略资源 Tooltip 追加队友战略资源清单；处理 TPT_NO_TRADING_LUXURIES/TPT_NO_TRADING_STRATEGICS/SETTINGS_DIPLOMATIC_DEAL 三个交易限制配置与队伍/FFA 检测。注册：ReplaceUIScript(LuaContext=TopPanel, LoadOrder 100000) 投递 TopPanelExt.lua（基类探测 include TopPanel_Expansion2/1/TopPanel 后覆盖 RefreshYields/LateInitialize/RefreshResources）+ ImportFiles(100010) + AddUserInterfaces(Context=InGame) 挂 TopPanelExt_TT.xml（Tooltip 类型+实例模板）+ UpdateText。优化：奢侈品类型查表 O(1)（替代 1.67 数组线性遍历）、战略资源表轻量化 {Index,Hash}、队友列表每刷新周期缓存一次（替代每资源循环内 GetAliveMajorIDs）、LocalPlayer/Diplomacy 引用提升、文本预加载缓存 + 带参数 tag 拆 PRE/SUF、取消 1.67 CanRefresh 冻结缺陷（每次悬停发最新数据）。】
- 交易限制与外交限制【已实现为条目10：移植 1.67 DDV 整个模块——①交易设置四模式参数（常规=仅禁城市/经典=禁金币+城市/独立=全禁仅放行奢侈品/自定义=8 个 TPT_NO_TRADING_* 禁止项+无代表团，ParameterDependencies 仅自定义可见）+ ②无友谊（TPT_NO_FRIENDSHIP 默认开）+ ③和解设置三模式（无法和解/允许和解/默认）；执行层：DiplomacyDealView_MPT.lua 经原版 include("DiplomacyDealView_", true) 通配注入交易界面上下文（Base:3177/Exp2:3229），按限制结果覆盖 PopulateAvailable* 八类（被禁返回 1 防无法签同盟，Favor/Gold 不隐藏容器其余隐藏）。变量名优化与 TopPanel 兼容：新增统一解析器 MPT_TradeRules.lua（MPT_ResolveTradeRules() 返回八键布尔表，预设优先于自定义布尔，语义同 1.67），替代 1.67 的 8 个松散全局 isXxxTradingAllowed，TopPanel（条目9）与 DiplomacyDealView（条目10）两上下文各自 include 共用（游戏内 include mod VFS 文件可靠，前端 include 缺陷不涉及）；同时修复条目9 移植时 isLuxuriesTradingAllowed/isStrategicsTradingAllowed 恒 true 占位未接 GameConfiguration 的问题（接线 tradeRules.Luxuries/Strategics，引用零改动）。无代表团/无友谊/和解经本 mod 首个 ActionCriteria（ConfigurationValueMatches 按 GameConfiguration 值）条件执行 SQL（删 DIPLOACTION_DIPLOMATIC_DELEGATION/DECLARE_FRIENDSHIP/PROPOSE_PEACE_DEAL 三表行 + DIPLOMACY_WAR_MIN_TURNS 改 9000 或恢复 10，LoadOrder 20001/999999）；参数定义与 DealView 导入门控 Disable_MPH（ModInUse inverse MPH 619ac86e，防 Parameters 主键冲突），文本双环境注册不门控。注册：FrontEnd UpdateDatabase×2+UpdateText，InGame ImportFiles(1010)+UpdateText+UpdateDatabase×4（criteria），Version 28】
- 游戏内玩家标记面板【已实现为条目11：移植条目4.4 PlayerMarkPanel 到游戏内（自定义功能，非 1.67 移植项）——`InGame/PlayerMark/MPT_PlayerMark.xml` 整体复制 StagingRoom.xml 条目4.4 区块零尺寸改动（1024×parent 全高面板、左右列布局、添加弹窗；确认框自研 MPT_PMConfirmPopup——引擎内置 PopupDialog 模板在 AddUserInterfaces 空 Context 不实例化报错，条目11 修复），`MPT_PlayerMark.lua` 复制 4.4 面板逻辑（存储层改为 include Shared/MPT_DataStorage.lua——游戏内 include mod VFS 文件可靠，Shared 独立文件本就是为游戏内消费方保留，不再复制第三份内联副本；Modding 组存储 API 游戏内可用·用户实证，与前端 4.4 同一份 [MPT_DS][MPT_PlayerInfo][Players] 存档双向互通）。打开方式：QuickPanel 展开区新增第三枚按钮「玩家标记」→ LuaEvents.MPT_PlayerMark_Toggle（展开高度 110→146、QuickSepBottom 108→144）；关闭走 X/点面板外（ModalBlocker）/ESC（ContextPtr:SetInputHandler，EndGameMenu 同款，添加弹窗在时先关弹窗）。与前端差异：删槽位热区联动 OnSlotNameClick、删 4.8 缓存刷新调用、删 ResetOnExit（游戏内退回主菜单即销毁 UI 状态）、Open/Close 首尾带 ContextPtr:SetHide（AddUserInterfaces 上下文默认 isHidden=true）。注册：AddUserInterfaces(900)+ImportFiles(900，含 Shared 两存储文件入 VFS)+UpdateText(100，与前端同一批 PlayerMark 文本双环境注册)，Version 29】
- 强制结束回合按钮与游戏内设置面板【已实现为条目12：移植 1.67 FEB（ForcedEndButton）+ Settings（简化版）——①FEB：ActionPanel 右下角 50×50 按钮，左键 UI.RequestAction(ACTION_ENDTURN, REASON="UserForced") 强制结束回合，右键保留 LuaEvents.ForcedEndTurn 触发点（1.67 由 NHK 热键系统消费，本 mod 未移植，预留接口）；加 m_attached 幂等守卫防 LoadScreenClose 重复挂载；显隐默认隐藏，由设置广播 TPT_Settings_Toggle("ForcedEndButton_Show") 控制。②设置面板：参数表 MPT_Settings（仅 ForcedEndButton_Show 一行，ParameterId 保留 1.67 key；表名避开 1.67 TPT_Settings 无 IF NOT EXISTS 的共存建表冲突），面板移植 1.67 TPT_SettingsPanel.xml（380×512 居中 + CheckboxInstance，删齿轮入口、底部 Header 改名 HeaderBottom）；入口为 QuickPanel 展开区新增第四枚按钮「设置」→ LuaEvents.MPT_Settings_Toggle（展开高度 146→182、QuickSepBottom 144→180）；无存档时首次自动弹出。注册：AddUserInterfaces(900)×2 + ImportFiles(900)×2 + UpdateDatabase(10) + UpdateText(100)，Version 35】【条目12修复：设置存档并入 MPT_PlayerInfo 复合组（Settings.ForcedEndButton_Show，与玩家标记 Players/隐身 HiddenSqlMark 同一 ModGroupName 单组行承载；SaveComposite 全量覆盖语义 → 前端 StagingRoom 2 处 + 条目11 2 处 + 设置面板 1 处全部写入口改为读回合并防互相覆盖）；点复选框不再立即落盘（回 1.67 仅 Confirm 保存——点击即存触发 StorageCreateCleanGroup 批量禁用/启用全部 mod 致卡顿），Version 36】【条目4.3重构：MPT_DataStorage 统一多表存储——旧 5 API（SaveData/LoadData/DeleteFile 单 key 组 + SaveComposite/LoadComposite）删除，新 3 API（LoadAll 整组读 / GetTable 单表读 / SaveTables 按表名覆盖写，内部自动读回合并、调用方零 merge）；数据格式 [MPT_DS][MPT_PlayerInfo][len]return { 表名=数据 } 单组行承载，表名 = 参数 ParameterId（ForcedEndButton_Show / HiddenSqlMark / Players），未来新功能 = 新表名；前端内联副本与 Shared/MPT_DataStorage.lua 同步重写，12 处调用迁移（前端 StagingRoom 5 + 条目11 4 + 设置面板 3）；不兼容旧存档（用户确认），Version 37】【条目12优化：设置面板独立目录 InGame/SettingsPanel/（git mv，与 ForcedEndTurn 拆分：FEB 按钮 tooltip LOC_FORCEEND_TT 留 ForcedEndTurn，设置项文本独立 SettingsPanel_zh/en.sql）；命名规范化 TPT→MPT（广播事件 LuaEvents.TPT_Settings_Toggle→MPT_Settings_Toggle、TPT_Settings_CheckBoxs→MPT_Settings_CheckBoxs、CreatCheckBoxStack→MPT_SettingsPanel_CreateCheckboxes、OnCheckBoxs→MPT_SettingsPanel_OnToggle、SetCheck→MPT_SettingsPanel_ApplyAll、storageData→MPT_SettingsPanel_Save、Initializedata→MPT_SettingsPanel_Load、OnShow/Close/SettingButton→MPT_SettingsPanel_Show/Close/OnOpen、XML ID TPTSettingsPanel→MPTSettingsPanel、文本 tag LOC_SHOW_FEB_*→LOC_MPT_SETTINGS_SHOW_FEB_*）；事件改名后与 1.67 广播互不联动（共存时各自控制各自 FEB）；历史功能 TPT 命名（DealRestriction 参数 key、TPT_PlayerData 表等）不在范围；modinfo 路径/Text action 拆分（IG_MPT_Settings_Text 新增）、Version 38】【条目12修复：删首次自动弹窗（1.67 First_Use 引导——本 mod 有 QuickPanel「设置」按钮明确入口，自动弹窗依赖本地存档判定且游戏内不可靠，此前每次进游戏都弹）；StorageCreateCleanGroup 禁用/恢复包 pcall（游戏内 InGame Context 的 DisableAllMods 流程不可用/抛错时静默退化为直接建组，修复存档写入失败根因——设置/玩家标记持久化受益；Shared+内联副本同步），Version 39】【条目12修复：设置面板开关入口独立事件名 LuaEvents.MPT_SettingsPanel_Toggle（QuickPanel「设置」按钮触发，无参）——原与参数广播 MPT_Settings_Toggle(ParameterId, Value) 共用事件名，点复选框广播时面板自身 OnOpen 监听响应导致面板立即自关闭；拆分后 MPT_Settings_Toggle 仅作参数广播（FEB 按钮消费），Version 40】

- 游戏内自动更新【已实现为条目13：移植 1.67 Update/AutoUpdate 纯逻辑部分（变量名 TPT→MPT 优化）——进游戏（空 Context 同名 Lua 自动执行，AddUserInterfaces Context=InGame，同条目5 RMC 机制）即对本局已启用的非官方创意工坊 mod 触发更新检查（复用条目4.7 优化：单次遍历建 [modId]=SubscriptionId 映射 + nil 防御 + 非官方过滤 + print 留痕，替代 1.67 O(n²) 双重循环）；并确保本 mod 已启用（EnableMods 按本 mod GUID 匹配 Handle——本地 mod 无订阅 ID，1.67 按 SubscriptionId 匹配不适用）；退出到主菜单（Events.ExitToMainMenu）时触发本 mod 工坊更新（按 MPT_MOD_ID 定位安装列表取 SubscriptionId，发布后自动生效，无订阅 ID 跳过——不依赖硬编码订阅 ID 常量）。不移植：更新内容 Tooltip 展示（Get_TPT_Update_Text/Creat_TPT_Update/OnLoadScreenClose——条目3.5 前端更新公告面板已有，本 mod 不覆盖 WorldTracker 标题控件）、固定订阅列表更新（1.67 特有配置）。注册：AddUserInterfaces(900) 仅 xml + 同名 lua 自动执行 + `<Files>` 登记，无文本（纯静默逻辑）。Version 42】
- 真实科文进度【已实现为条目14：移植 1.67 TCP（Tech And Civic Progress）——科技树/市政树/研究选择器/市政选择器显示真实研究进度：Tooltip「研究进度 x / y + 获得提升后预估 + 可凭提升完成」、可凭 boost 完成的节点/条目名追加 [icon_You]、modifier 附加科技/文化加速计入（EFFECT_ADJUST_TECHNOLOGY_BOOST / EFFECT_ADJUST_CIVIC_BOOST）、BLOCKED 状态回合数按实际产出重算。对比结论：1.67 基于 Base 整文件复制 + 函数内修改 + 尾部补抄官方 Exp1（联盟研究图标）/Exp2（IsSearchable）——本 mod 改为 include 探测官方最高版本（TechTree_Expansion2→1→Base / CivicsTree_Expansion2→Base）自动获得官方功能，零复制；共享脚本 TechAndCivicSupport.lua 被 7 个 context include 只能整文件同名覆盖（ImportFiles 100010 压过 1.67 的 2001）。优化：删 1.67 调试代码、函数 MPT 前缀、缓存变量 local 化、文本预加载缓存拆 PRE/SUF、去冗余 Boosted 字段（用原版 BoostTriggered）、「可凭 boost 完成」图标走数据层（GetResearchData/GetCivicData 直接给 kData.Name 追加图标，ResearchChooser/CivicsChooser 显示自动生效**无需替换 context**——1.67 需改 4 个文件）、boost 静态表 MPT_BoostMap 顶层一次构建 O(1) 查表（替代 1.67 刷新静态数据 PopulateItemData + OnOpen）、GetExtraBoostFromModifiers 每回合缓存。默认启用无开关（同条目9 TPE 风格）；与 1.67 共存时本 mod LoadOrder 100000 压过。注册：ReplaceUIScript(100000)×2（TechTree/CivicsTree）+ ImportFiles(100010)（TechAndCivicSupport + 2 MPT lua）+ UpdateText(100) 双语言。Version 43】
- 队友资源可见性显示【已实现为条目15：移植 1.67 STR（STR/UI/WorldViewIconsManager_STR.lua）——同队队友已通过科技/市政解锁、而本地玩家尚未解锁的资源，在地图已探索迷雾格显示迷雾样式图标（条目9 顶部面板「队友战略资源清单」的地图侧对应能力）；区域拍下盖住资源时按可见性刷新该格、总督任命（Exp1+，结社/总督解锁场景）一次性补扫、外部刷新接口 LuaEvents.MPT_WorldViewIcon_Rebuild（1.67 TPT_WorldViewIcon_Rebuild 的 MPT 命名版）。实现：ReplaceUIScript(LuaContext=WorldViewIconsManager, LoadOrder 100000) 投递 InGame/TeamVisibleResources/WorldViewIconsManager_MPT.lua（include 原版后覆盖 Initialize/GetNonEmptyAt/OnResearchCompleted/OnCivicCompleted/OnShutdown）+ ImportFiles(100010)；原版该文件仅 Base 一个版本（DLC/资料片无变体，已验证），无需版本探测（区别条目9/14）；目录名用描述性命名对齐 InGame 规范（非 1.67 缩写 STR）。优化：①观察者/热座 nil 防御（条目7 观战场景，1.67 每回合 Players[localPlayer]:GetTeam() 报错）②1.67 两个 90% 重复扫描函数合并为 MPT_UpdateTeamVisibleResources（Governor 版保持一次性自移除语义）③解锁科技/市政表 [Type]=Index O(1) 映射 + 未解锁资源轻量 {Index,Hash}（替代数组线性扫描/整行存储）④本地玩家自己已可见的资源剪除出扫描表（1.67 永久残留致空表早退失效）⑤LoadGameViewStateDone + Initialize 尾部立即同步（1.67 读档/中途加入后要等首回合结束才首次扫描）⑥OnShutdown 覆盖卸载全部监听（1.67 注册后从不清除）⑦GovernorAppointed 仅 Exp1+ 存在，挂载/卸载前 nil 守卫。无文本无 XML（图标 Tooltip 全复用原版 tag）。Version 44】
- 禁用地图钉【已实现为条目16：移植 1.67 RMP（Remove Map Pins，目录 `InGame/DisableMapPins/`，描述性命名对齐 InGame 规范）——高级选项新增金色布尔开关「禁用地图钉」（CPL_NO_PINS，默认关、开局后不可改、SortIndex 2902，ParameterId 沿用 1.67 原名与 1.67 同装时共用同一开关行，同条目10 保留参数名先例；文本 tag 同样沿用 CPL_NO_PINS_NAME/DESC，中文照抄 + 英文本 mod 补全），勾选后进局：①LoadScreenClose 隐藏左下角小地图「地图钉列表」按钮（DisableMapPins.lua，函数 MPT 前缀 + LookUpControl nil 防御）②空壳整体替换原版 MapPinListPanel.lua（多人联机卡顿元凶，MapPinListPanel_MPT.lua 经 ReplaceUIScript LoadOrder 100000 压过 1.67 同类替换的 2010 + MapPinListPanel.xml 空壳同名覆盖布局双保险）；地图钉本身（旗子渲染/放置）不受影响。三个 InGame action（ReplaceUIScript/ImportFiles/AddUserInterfaces）均由 No_Map_Pins criteria（ConfigurationValueMatches Game/CPL_NO_PINS/1，criteria id 镜像 1.67）门控——不勾选时零改动原版；参数 FE UpdateDatabase(10) 无条件注册（复选框常在）+ 文本 FE UpdateText(100) 仅前端注册（游戏内无本条目文本）。不移植：1.67 NT 模块的进局通知「地图钉已禁用」（属 NT 而非 RMP，最小范围）。Version 46】
- 建立城市免确认【已实现为条目17：移植 1.67 RCT（移除建立城市弹窗，目录 `InGame/InstantFoundCity/`，描述性命名对齐 InGame 规范）——点击「建立城市」不再弹确认弹窗（含「将移除地貌」提示），直接坐城；无条件常开同 1.67（用户确认不加开关）。机制：ReplaceUIScript 抢占 UnitPanel 上下文入口（LoadOrder 100000 压过 1.67 同类替换的 11000 + ImportFiles 100010，均无 criteria）→ 文件内手动重建原版 include 链（BSM 观战 unitpanel_spec 优先 → XP2 → XP1 → Base，include 对缺文件仅告警、命中全局 Initialize 即 break 防重复订阅；BSM 的 unitpanel_spec 优先级无法用 modinfo criteria 静态表达，运行时自适应更稳）→ 重定义全局 OnUnitActionClicked_FoundCity（原版按钮回调是匿名闭包，点击时才解析全局名，include 后重定义即生效，无需重建动作表；函数名保持原版全局名不可 MPT 化）。优化：①自建 local MPT_HexColoringWaterAvail（UILens.CreateLensLayerHash，原版引擎拼写 Availablity 照抄）修复 m_HexColoringWaterAvail 原版 UnitPanel.lua 顶部 local 跨 include 不可见致 nil——1.67 尾部 UILens.IsLayerOn(nil) 报错被引擎吞掉，坐城后「可坐城水域」高亮层不关闭、镜头不回默认 ②删 popupString 两次 Locale.Lookup 死代码（kResults 分支整体删除，参数仅兼容原版调用签名保留）③注释尸体清理函数重写，原版弹窗逻辑以修改留痕横幅保留 ④include 链注释补全 + print 加 MPT 前缀。无文本无 XML 无参数。Version 49】
- 城市横幅刷新【已实现为条目18：移植 1.67 PCF（pk-civ6 皮皮凯「城市横幅刷新修复」，目录 `InGame/CityBannerRefresh/`，描述性命名对齐 InGame 规范）——修两件事：①原版 CityBannerManager 不订阅 CityWorkerChanged（市民变动后横幅产出滞后）→ 延时去抖后调原版全局 RefreshBanner（UpdateStats+UpdateRangeStrike+UpdateName）单城刷新；②城界扩张/人口增长/地块产出变化（改良建成/被劫/修复）后市民不自动重排 → 延时去抖后下发 CityCommandTypes.MANAGE 公民指令（原版 PlotInfo 点击市民同款命令，坐标取城市中心），促使引擎把市民重新分配到最优地块。注入机制：文件名前缀 `CityBannerManager_` 被原版 CityBannerManager.lua 末尾官方预留通配 `include("CityBannerManager_", true)` 拉入同上下文（同条目7 EndGameMenu 机制），可直接调用上下文全局 RefreshBanner，modinfo 仅 ImportFiles(1010) 无需 LuaContext（1.67 同款纯 ImportFiles，其 LoadOrder 7 互不干扰；与 1.67 同装两文件都会被拉入、重复执行无害，建议二选一）。刷新机制重写（替代皮皮凯 LuaTimer，用户要求探索更高效刷新方式）：①单订阅调度器——1.67 每挂一个定时器就 Add/Remove 一条 GameCoreEventPublishComplete 订阅反复增删，本文件全局仅 1 条订阅驱动任务表（[taskID]={Deadline,Func,Values,Unpack}）先出表再执行、O(1) 增删 ②真实单调时钟 UI.GetElapsedTime（原版 PlotToolTip 5 秒超时同款，缺 API 兜底 os.clock）替代 1.67「发布周期计数」冒充秒数（事件批节奏不匀，2 周期可能 0.2s 也可能 10s）——2 秒即真 2 秒 ③全 local 零全局污染（1.67 AddTimer/CallbackDict 等 6 个全局与同上下文 CSB/CSA 补丁有冲突风险）④去重键统一 MPT_Refresh_/MPT_Rearrange_ 前缀命名空间（1.67 PlotYieldChanged 路径裸 cityID 当键可能互吞）⑤重排指令后兜底挂一次横幅刷新（1.67 仅依赖重排引发 CityWorkerChanged，引擎未发布时横幅滞留旧值；同键去抖不增调用）⑥防御补全：重排前城市 nil 检查（延时期间被毁/易主）、PlotYieldChanged 地块 nil 检查、观察者天然跳过。保留 1.67 语义：本地回合开始 1 秒冷却（官方回合切换整批刷新期间跳过）、本地回合结束清空未到期任务、重排 4 秒/产出 2 秒/刷新 2 秒去抖。无文本无 XML 无参数。Version 50】【条目18修复：MPT_GetTime 以 cfunction（`UI.GetElapsedTime or os.clock` 直接赋值）赋给 ifunction 标注变量——引擎类型检查报 Type check failed: expected 'ifunction', but got 'cfunction' 致文件 chunk 中止、通配 include 失败、全部功能未加载（横幅毫无反应根因，Lua.log 实证）；改为 Lua 闭包包装（调用时探测 API，MPT_ClockName 留痕时钟选择）+ 全关键路径调试 print（MPT_Banner: 前缀：文件加载留痕/回合冷却与清任务/本地玩家事件到达与冷却态/任务排程与执行/MANAGE 指令下发，实测确认后删）；Version 51】【条目18调优：去抖延时 1.67 的 4/2/2 周期计数在真实时钟下过慢——横幅刷新 2 秒→0.1 秒近实时（用户要求 100ms 左右；实际执行时刻=0.1 秒后首个 GameCoreEventPublishComplete tick，事件到达后本就紧跟批结束，体感即近实时）、市民重排 4/2 秒→0.5 秒统一合并窗口，延时收敛 MPT_REFRESH_DELAY/MPT_REARRANGE_DELAY 两常量便于调整；Version 52】【条目18清理：移除全部调试 print（MPT_Banner: 前缀 10 处——文件加载留痕/回合冷却与清任务/本地玩家事件到达与冷却态/任务排程与执行/MANAGE 指令下发），实测确认功能生效后删；仅为包 print 存在的 3 个 `if playerID == Game.GetLocalPlayer()` 块与仅被加载留痕 print 消费的 MPT_ClockName 一并删除，逻辑零改动；顺带入库 MPT_REARRANGE_DELAY 0.5→0.1（用户手调）；Version 54】
- 清理通知按钮【已实现为条目19：移植 1.67 NOC（目录 `InGame/NotificationClear/`，描述性命名对齐 InGame 规范）——通知栏插入复用原版 ItemInstance 通知条目模板的「垃圾桶」按钮（QueueTrash 图标、隐藏计数/翻页箭头/页点），点击一键清理全部可手动关闭的通知（NotificationManager.Find→CanUserDismiss→Dismiss，不可关闭保留）。注入：文件名前缀 `NotificationPanel_` 走原版通配 include 钩子进 NotificationPanel 上下文（同条目7/18，ImportFiles 1010 无 LuaContext）；链式覆写全局 OnDefaultAddNotification/ReleaseNotificationEntry（原版 MakeDefaultHandlers 对 handler.Add 延迟绑定，include 时重定义全局即生效；函数名保持原名；BASE_ chunk local 捕获零全局污染）。按钮位置（用户询问 Stack:SortChildren 能否置底）：无需采用——通知栈 Anchor R,B+StackGrowth Top（1 号子控件在最底部）、按钮先于首条通知实例懒创建恒为 1 号子控件、且永不被释放（原版 ClearNotifications/ReleaseNotificationEntry 只清理 m_notifications 登记条目，实例复用不影响未释放实例）→ 天然恒居最底部零代码保证；SortChildren 签名是比较函数非 index，O(n·logn) 重排+重布局对固定位置按钮纯属开销（InstanceManager.lua 官方注释并警告其破坏布局依赖样式）。优化：①精确存活集 [playerID][id]=true+计数替代 1.67 环形缓冲（100 上限回绕静默丢最老 ID/已释放 ID 永久残留重查）②显隐用自维护计数 >0（替代直读 m_genericItemIM.m_AllocatedInstances 内部字段+「>1 因按钮占位」魔数）③单清理回调仅注册 MouseInArea 左右键（删 MouseOutArea 重复注册的同点击双触发隐患）④设置广播 nil 双守卫（1.67 在任何通知出现前收到广播 NPQC_ClearButton.Top 崩脚——本 mod 面板 LoadScreenClose 即全量广播此场景必现）⑤Clear 先快照 ID 再 Dismiss（防同步释放回调遍历中改表）⑥零全局污染。开关：条目12 MPT_Settings 表加行（ParameterId 沿用 1.67 原 key NotificationPanel_QuickClear，反向语义同 1.67：勾选=禁用按钮，默认 0=显示；文本 LOC_MPT_SETTINGS_NOC_DISABLE_* zh+en，1.67 仅 zh），lua 订阅 MPT_Settings_Toggle 匿名闭包，初始状态经面板 LoadScreenClose ApplyAll 全量广播自动送达无需自读存档。tooltip 沿用 1.67 借用 tag LOC_HUD_MAP_SEARCH_CLEAR_SEARCH 零文本注册。与 1.67 同装时 NOC/NDR/本文件三个 NotificationPanel_* 通配共载（覆写链依次生效功能正常）但会双按钮，建议二选一。Version 53】【条目19修复：通知清空后垃圾桶按钮单独残留——计数只增不减五处漏减堵漏：A【主犯】原版 ClearNotifications 以 ipairs 遍历 m_IDs 而 ReleaseNotificationEntry 内部 table.remove 使后续左移（每隔一个跳过一个），NotificationRefreshRequested/换人/观战路径随即 DestroyInstances 整批销毁，被跳过 ID 永不走 Release→计数卡 >0 而 UI 已空——包裹原版 ClearNotifications（4 个整批清空路径公共出口，调用点运行时动态查找）BASE 后统一清账 B 原版 OnUnitKilledInCombat 笔误（playerId 应为 playerID）兜底清理 100% 失效、此类移除无 Dismissed 事件 C 换人/观战后旧玩家 ID 被 OnNotificationDismissed「非本地玩家」分支整体 return——新增 MPT_Reconcile 对账：逐 ID NotificationManager.Find 剪除引擎已消失通知（B/C 自愈），Add/Release 覆写尾统一调用，计数重算单一事实不再手动 ±1 D BASE 提前返回（typeName nil DataError/重复 ID return nil）时孤儿 ID——Add 登记前 GetNotificationEntry 查册，面板未收下不登记 E 口径假阳性：回合阻塞类通知（未选科技等）有册无 UI 实例（原版 754 行门槛），显隐改按「有 UI 实例资格（IsIconDisplayable 且非回合阻塞）」的存活数（=1.67 实例计数语义）。附带修复：DestroyInstances 两路径（RefreshRequested/观战）连按钮一起销毁但引用非 nil 致懒创建守卫永不触发按钮永久消失——包裹 OnNotificationRefreshRequested/OnStartObserverMode 置 nil 引用懒重建（重建时栈空仍为子控件 1 号恒居底部；换人路径不销毁实例故不置 nil 防存活按钮变孤儿重复建）；事件订阅发生在原版 LateInitialize 晚于通配注入、Add() 捕获的已是包裹版；Version 55】
- 智能回合计时器【已实现为条目20：移植 1.67 CC/Timer TimerPro + NHK/UI/TurnTime_HotKey 的热键与按钮部分（目录 `InGame/SmartTurnTimer/`，描述性命名对齐 InGame 规范；用户确认范围：聊天指令/PID平衡/宣战掉线加时要，15 秒滴答音不要，热键与加减时按钮一起移植；NHK 强制结束回合段落不移植——条目12 FEB 已承担）——房主在回合整体结束（Events.TurnEnd）时以「半数真人玩家结束回合时已耗回合时间」为反馈量做 PID 自动平衡（一阶滤波 0.5 → Kp 0.5/Ki 0.08/Kd 0.25 → 积分抗饱和+限幅 ±10 → 衰减 0.98 → 过时代前馈 GetNextEraCountdown 加 10/20 减 15 → 下限 30 秒取整）改写官方 TURN_TIMER_TIME 参数并 Network.BroadcastGameConfig 广播全房；宣战 +20s（大文明间）/剩余<10s 对城邦 +8s（防卡秒顶城邦）/掉线 +30s 自动加时；议会投票阶段全员开新回合后固定 120s；首回合多人强制 TURNTIMER_STANDARD 并以 30s 起步；聊天指令 p+（+20s 剩余<8s 改 +24，每回合一次）/p+++（本回合无计时，下回合恢复）/p--（下回合 -15s）仅房主监听公共频道；聊天框旁 P++/P-- 按钮（所有玩家，点击发聊天指令，房主由自己监听代执行）+ [ ] 加减时热键（GameInfo.InputActions+InputActionDefaultGestures 注册，]/[ 键位，CategoryId=ONLINE/ContextId=World 沿用 1.67；房主直接改配置广播，非房主代发 p++/p--）。加载：空 Context 经 AddUserInterfaces(900, Context=InGame)+ImportFiles(900) 同名配对（同条目12/13 机制），1.67 的 TimerPro/TurnTime_HotKey 两 Context 合并单文件；按钮 ChangeParent 到 /InGame/WorldTracker/ChatPanelContainer 在 LoadScreenClose 后执行（1.67 同款时序，防 WorldTracker 未加载 LookUpControl nil）；热键 InputActions 前后端各注册一次（FE 主菜单按键绑定界面显示名称/描述）。开关：条目12 MPT_Settings 表加行（ParameterId 沿用 1.67 原 key TOOLS_COMMAND，默认 1=开启 1.67 同款；文本 LOC_MPT_SETTINGS_TIMER_ENABLE_* zh+en），lua 文件加载即订阅 MPT_Settings_Toggle（早于面板 ApplyAll 广播必达），语义差异：1.67 为全局 Game 参数广播全房 → 本 mod 每玩家本地开关（房主控自动平衡，非房主各控自己按钮显隐/热键代发），1.67 房主热键不受开关限制 → 本 mod 统一由本机开关门控。优化：①零全局污染（1.67 十余个全局函数/变量全 local + MPT_Timer_ 前缀）②魔法数 2133509568 → DB.MakeHash("TURNTIMER_NONE")（原版 ActionPanel 同款 Hash 参数比较）③四处重复真人全遍历合并单一 CountHumans（一次遍历返回总数+已结束数；首回合初始化人数判定也复用，1.67 该处未排除观察者已修正）④PID 控制量只算一次复用+手动修正量懒计算⑤nil/类型防御（TURN_TIMER_TIME 非 number 兜底 60/PlayerConfigurations/Players/Game.GetEras 守卫/聊天文本 type 守卫——1.67 直读 nil 参与算术有崩脚隐患）⑥冷却 os.time 墙钟 → UI.GetElapsedTime 单调真实时钟闭包包装（条目18修复同款）⑦IsAnyMultiplayer 联机门控（1.67 单人局也空跑平衡并广播）⑧TurnTimerUpdated 双订阅（记录+15s 音效）单订阅仅记录⑨按钮显隐 Events.GameConfigChanged → MPT_Settings_Toggle，XML 默认 Hidden 防闪现⑩修正 1.67 减时按钮 tooltip「下回合减少20秒」与实际 -15s 不符的文本 bug。与 1.67 同装注意：双方计时器都回合末改写 TURN_TIMER_TIME 广播互相拉扯致振荡（用户确认不管控），勿同开。无 AddGameplayScripts 纯 UI 侧。Version 56】【条目20扩展：新增四模式工作模式 Game 配置参数 MPT_TIMER_MODE（`Config_SmartTimer.xml`，FE UpdateDatabase(10)——Configuration 参数须前端注册、游戏开局建库继承，条目16 CPL_NO_PINS 同款，IG 重复注册主键冲突），GameOptions 组下拉四选一（SortIndex=21 紧随原版回合计时器类型 20，ChangeableAfterGameStart=1 房主可开局后暂停菜单切模式广播全房，DomainValues 表结构经原版 SetupParameters.sql 核实 (Domain,Value,Name,Description,SortIndex)、无需 Domains 行）：OFF 关闭所有计时器（mod 完全不介入）/ BASIC 基础只包含聊天指令（p+ 立即加时、p+++ 本回合无计时下回合恢复、p-- 下回合直减 15 秒下限 30 秒——回合末最小落实逻辑，否则 p+++ 在无平衡逻辑的基础模式会永久关掉计时器）/ SMART 智能计时器（初版完整功能：PID+聊天+宣战掉线+投票 120s+过时代前馈）/ FIXED 固定计时器（纯回合阶梯曲线：初始 30 秒、30T 起 80s、50T 起 120s、70T 起 180s 后保持；不含聊天指令与任何自动调节；开局 LateInitialize 即强制 STANDARD+首回合 30 秒使「初始 30 秒」从第 1 回合生效，回合末按下一回合 CurrentTurn+1 查 MPT_FixedTiers 阶梯表）。默认 SMART（延续默认开决策）；初版 MPT_Settings 布尔行 TOOLS_COMMAND 与 SettingsPanel 文本回退删除（布尔无法表达四模式，两套开关并存会互相打架），语义差异第 1 条改回：1.67 布尔 TOOLS_COMMAND → 本 mod 四模式 Game 配置参数，全房统一跟随房主配置（回归 1.67 全局语义）；lua 删 MPT_TimerEnabled 与 MPT_Settings_Toggle 订阅 → MPT_Timer_GetMode()（type 守卫参数异常兜底 OFF fail-safe）+ MPT_Timer_IsCommandMode（BASIC|SMART 供聊天/按钮/热键门控）+ Events.TurnEnd 入口按模式分发（FIXED→固定曲线/BASIC→指令落实/SMART→PID/OFF→仅清状态），宣战/掉线/投票/半数采样门控改 ==SMART，按钮显隐恢复 Events.GameConfigChanged 随配置广播刷新；模式文本（参数 Name/Desc+四值 Name/Desc 共 10 tag×2 语言）入 SmartTurnTimer 双语 SQL。Version 61】【条目20扩展2：拆分两设置+阶段计时器平滑曲线（用户需求：布尔「启用聊天指令」与三值模式「关闭/智能计时器/阶段计时器」分开，曲线在回合节点间线性增加，节点数据入 SQL 表）——①MPT_TIMER_CHAT 启用聊天指令（布尔 Domain=bool 默认 1，SortIndex=22）：p+/p+++/p- 与按钮/热键总开关，原 BASIC 模式语义由「任意模式+此开关」组合表达（1.67 固定曲线不含聊天的约束随拆分取消，TIERED 亦可开指令）；②MPT_TIMER_MODE 三值 DomainValues（删 BASIC，FIXED 改名 TIERED 阶段计时器）：OFF 全关（指令即使开也不生效）/SMART PID 完整/TIERED 回合节点平滑线性曲线——新增 `SmartTurnTimer_Tiers.sql`（仅 IG UpdateDatabase(10)：MPT_TimerTiers 表 Turn 回合数/Time 秒数，主键 Turn，默认节点即用户给定示例 (30,30)/(50,80)/(70,180)=1~30T 保持 30 秒、30~50T 升至 80 秒、50~70T 升至 180 秒后保持；自定义曲线直接改 VALUES 行，脚本自动按 Turn 升序插值无需改代码），Lua MPT_Timer_GetTierNodes 懒加载缓存（开局建库后静态）+行值 type 守卫，MPT_Timer_TieredTimeForTurn 相邻节点线性插值（首节点前保持首值/末节点后保持末值/表空兜底 60），TIERED 回合末按下一回合 CurrentTurn+1 取曲线值+聊天指令的下一回合效果在曲线值上落实（p+ 顺延 +5/p-- 减 15 与 SMART 手动修正量同幅、p+++ 恢复 STANDARD、下限 30 秒取整广播），开局 LateInitialize 即强制 STANDARD+首回合取曲线在回合 1 的值；lua 删 MPT_MODE_BASIC/IsCommandMode → MPT_Timer_ChatEnabled()（布尔开且模式非 OFF）门控聊天/按钮/热键，TurnEnd 分发改 SMART/TIERED/OFF，宣战/掉线/投票/半数采样仍仅 SMART。Version 75】
- 商路界面增强【已实现为条目22：移植 1.67 BTS/ 目录（BTS = Better Trade Screen Lite，Astog 原作 + 1.67 TeamPVPTools/DeepLogic 魔改，目录 `InGame/BetterTradeScreen/`，描述性命名对齐 InGame 规范；用户需求：移植并优化，解决「商路目标面板收益没有及时刷新」）——①商路总览侧边面板（原版 LaunchBar 按钮打开）：三页签（我的商路/通往我城市的商路/可用商路）+ 按出发/目的城市分组与组内展开收起 + 七级排序栏（左键插入右键删除 Shift 多级）+ 按文明/城邦/贸易任务筛选 + 每行真实收益双列 + 在途商路精确剩余回合（Tracker 本地账本按回合递减、读档经 PlayerConfigurations 持久化重建）+ 旅游业绩/外交可见性/贸易站标记 + 空闲商人「选择商路」按钮与商人传送；②商路目的地选择面板：候选目的地列表（真实收益/回合数/贸易站/城邦任务图标）+ 七级排序栏 + 筛选 + 商路透镜（全部候选路径/选中置顶）+ 上次商路自动预选；③商人传送面板（TELEPORT_TO_CITY）：横幅着色 + 城市字母序；④TradeSupport.lua 共享库（真实收益缓存体系 + Tracker + 排序打分），被总览/选择器两上下文按名 include。
  实现：原版四文件（TradeOverview/TradeRouteChooser/TradeOriginChooser/TradeSupport）均 Base 单版本无 DLC 变体（已验证）→ 整文件覆盖；三个 UI 上下文 ReplaceUIScript 100000 + TradeSupport.lua 纯 ImportFiles 遮蔽（文件名必须保持原名）+ 三 XML 同名覆盖布局，统一 ImportFiles 100010；与 1.67 同装压制其 BTS_IMPORT_FILES(11011) 本 mod 生效（文件头横幅留痕）。
  【核心修复·收益不及时刷新】1.67 根因：TradeSupport 收益缓存每回合只建一次（CacheRoutesInfo 的 m_Cache.TurnBuilt ≥ 当前回合闸），联机同时回合下开面板前发生的政策/建筑/贸易站/宣战议和变化全部显示旧值到下回合；且选择器重建触发面窄（仅换商人/跨回合/地块首次揭示）、政策变更只 Refresh 不清缓存。修复：两面板 Open() 先 CacheEmpty()（打开必新算）；OnPolicyChanged/Obsoleted 补 CacheEmpty；新增失效事件订阅（事件名均经原版 UI 使用验证）——yield 类 BuildingChanged/DistrictAddedToMap/TradeRouteAddedToMap（他方商路抵达建贸易站）清缓存+打开时 Refresh（各页签读缓存经 CacheTouchRoute 按需重算），validity 类 DiplomacyDeclareWar/DiplomacyMakePeace/TradeRouteCapacityChanged/CityAddedToMap 额外强制重建候选表（Chooser 置 m_TurnBuiltRouteTable=-1 命中重建条件、Overview 置 m_HasBuiltTradeRouteTable=false）；缓存本体保留（总览单次刷新数百行×12+ 引擎调用的性能护栏）。附带修复：①选择器 Open() 恢复上次商路引用未定义全局 DestinationCityID（应为 lastRoute.DestinationCityID，1.67 预选实际从未生效）②DeepLogic 可见性补丁 m_PlotRevealed 永不清空（探索-迷雾-再探索同格永不触发）→ 回合末清空 + 面板打开时立即 Refresh ③单数产量函数非缓存分支未定义标识符（kYieldMultipliers/pDestinationCity/kRouteInfo，1.67 中不可达未爆）④Tracker 持久化 key 笔误 Rotues→Routes（1.67 旧存档键不读取，读档按实际在途商路重建仅丢精确剩余回合计数）。
  原版 2.0 回迁（BTS 基于旧版原版）：弹窗互斥三事件 TradeRouteChooser_CloseIfPopups/ReOpen/WorldInput_MakeTradeRouteDestination + OnWorldInputMakeTradeRoute + CheckNeedsToOpen 的 UI.IsInGame() 守卫 + Open() 尾 LaunchBar_CheckPopupsOpen()（与条目8 WorldTracker 弹窗/条目12 设置面板共存）。
  裁剪（1.67 行为为准，用户未另行裁决按推荐执行）：商人自动化整体剔除（1.67 v1.34 已禁用 UI——RepeatRoute/FromTopSortEntry 复选框、AutomateTrader/FindTopRoute/RenewTradeRoutes/IsTraderAutomated 等底层与 TradeSupportAutomater_* 及 BTS_TraderAutomatedSettings 持久化、总览取消自动化按钮、XML 控件全不携带）；目的地列表对方收益列不恢复（1.67 v1.33 裁剪，顶部选中详情仍显示对方收益）。
  设置：4 个子选项不开放配置（初版曾收编条目12 MPT_Settings 表 4 行 BTS_*，条目22调整回退，见文末）：三个 Lua 的 MPT_Settings_Toggle 订阅与 OnSettingsChange 均不携带，GameConfiguration BTS_* 读取与 BTS_SettingsUpdate 事件也不移植；1.67 独立 BTS 设置面板不移植（连带规避其 ContextPtr:SetInputHandler(nil 未定义 OnInputHandler) 隐患），与 1.67 同装时其配置值无人读取（UI 文件本 mod 压制生效）。零散：1.67 遗留 dbug_print=true 与四处无条件 print 关闭。
  注册：modinfo 3×ReplaceUIScript(100000)+ImportFiles(100010)+UpdateText IG(100)（仅游戏内消费，同条目21 仅 IG 先例），Files 登记 9 文件。Version 62】【条目22调整：去除 4 个设置项（用户裁决「去掉那4个Setting，以 1.67 BTS 中的默认值为准」）——①三 Lua（TradeSupport/TradeRouteChooser_MPT/TradeOverview_MPT）删 MPT_Settings_Toggle 订阅与 OnSettingsChange 响应函数，四个选项局部变量改硬编码常量 = 1.67 BTS_Settings.sql 默认值（近似路径 BTS_ApproximateTraderPath=关/排序序号 BTS_ShowSortPriorities=关/全部路径 BTS_ShowAllRoutePaths=开/选中商人显示路径 BTS_ShowTraderPathOnSelection=开，初版本就取默认值故行为零变化，仅去可配置性与订阅开销）②MPT_Settings.sql 回退 4 参数行（表回 FEB/NOC + 后续条目行），SettingsPanel 双语删 8 tag（LOC_MPT_SETTINGS_BTS_*）③modinfo 条目22注释同步。Version 91】
- 城邦界面增强【已实现为条目23：移植 1.67 BCS/UI/（BCS = Better City States，1.67 内打包的城邦面板优化，仅 CityStates.lua/.xml 两文件）+ 并入观察者 mod Better Spectator Mod（BSM，工坊 1916397407）的城邦面板补丁（用户需求：移植并「直接兼容、包含观察者 mod 的内容」；目录 `InGame/BetterCityStates/`，描述性命名对齐 InGame 规范）——①任务行：列表每城邦行底部新增 CuiQuestGrid 显示当前任务（有任务白字任务串、无任务灰字 LOC_NOTIFICATION_CITYSTATE_QUEST_COMPLETED_MESSAGE），原版 QuestIcon 图标弃用（BCS 原样魔改）②单城邦视图默认页签改「影响力排名」（OpenSingleViewCityState 的 MODE.EnvoySent→MODE.InfluencedBy，BCS）③加成弹出区永久隐藏（ViewList 的 BonusArea SetHide(true) + XML BonusArea 高度塌缩 0，BCS）④宣战时使者排名条染砖红（AddInfluenceRow 的 IsAtWarWith 染色 (178,34,34)，BCS）⑤BSM 观察者补丁并入：MPT_IsSpectator() 本地助手（读 BSM Gameplay 脚本 Data/Spectator.lua 写入的 SPEC_NUM/SPEC_ID_k 游戏属性，未装 BSM 时 SPEC_NUM 为 nil 恒 false 行为同原版）→ GetCityStatesMetNum/ViewList 行过滤 isHasMet 放宽为「或观察者」（观察者不参与外交未遇见任何城邦，不放宽面板恒空；ViewCityState 左列 BSM 未放宽，保真不补）、GetData/AddInfluenceRow 的 CPL_ANONYMOUS 联赛匿名开关（仅匿名 HasMet 分支真实名，"YOU"/"UNKNOWN" 不动，BSM 原样）、OnLocalPlayerTurnBegin 面板隐藏时也持续 Refresh、Initialize 去 CAPABILITY_CITY_STATES_VIEW 门控（观察者无此能力会拦截面板初始化；SPEC_NUM 属性写入时机晚于 UI 初始化，无法用运行时观察者判定替代门控）——单文件直接兼容，替代 1.67 的 ModInUse_BSM criteria 双版本切换机制（1.67 常规版 CityStates_TPT.lua 1000/1010 + 检测到 BSM 时切换 CityStates_SPEC.lua 11000/11100，其 SPEC 版为缺 XP1/XP2 大使尾部的旧基线，MPT 合并版取 TPT 全量基线）。
  经 diff 核实不采纳的 BSM 改动（保真原则，文件头横幅留痕）：①OpenOverview/ViewList/GetData 三处注释掉 localPlayerID == -1 提前返回——BSM 观察者占用真实玩家槽位（localPlayerID 有效），-1 门控本就不触发，且 MPT_IsSpectator 依赖 SPEC_ID_k 与 GetLocalPlayer() 相等、-1 时恒 false，「非观察者才返回」为无效补丁 ②OnInputHandler 去掉 m_kScreenSlideAnim 关闭回调（OnClose）——ESC 关闭退化为 kAnim.Hide 丢失 Close() 的使节簿记与音效，Close() 自带 -1 防御与观察者无关 ③GetData 重排 bonus 新达成检查丢失 m_kLastCityStates[iPlayer] ~= nil 保护（m_kLastCityStates[iPlayer] 为 nil 时索引 nil 回归 bug，保留原版嵌套结构）④BSM 自报 print 与注释态调试 print。
  实现：原版 CityStates.lua 为 Base 单版本无 DLC 变体（XML 走 Base→XP1→XP2 同名替换链，已验证）→ CityStates_MPT.lua = 原版 Base + XP1 大使 + XP2 宗主加成三文件压平合并体（1.67 TPT 版原样，ReplaceUIScript 机制要求整文件替换上下文入口）+ MPT 头横幅 + 5 处 BSM 补丁（逐处留痕横幅，内联 bspec 块收敛为 MPT_IsSpectator 单助手）；CityStates.xml = BCS 版原样拷贝（相对 XP2 版仅 3 处语义差异：BonusArea 塌缩/CityStateRowInstance 行高 48→70 锚点 L,C→L,T/新增 CuiQuestGrid+CuiCityStateQuest，QuestIcon 注释保留）。注册：ReplaceUIScript IG_BCS_CityStates(100000, LuaContext=CityStates) + ImportFiles IG_BCS_Import(100010)（同条目15/17/21/22 模式），压过 1.67 两套注册（1000/1010 与 11000/11100），与 BSM 同装时其裸 ImportFiles(9999) 的同名遮蔽被 ReplaceUIScript 换上下文入口无效化（观察者功能已并入本文件，遮蔽文件成死文件）。BCS 无自定义 LOC tag 零文本文件。自检：luaparser 剥标注解析通过（顶层 187 语句）。Version 76】
- 外交丝带扩展【已实现为条目24：移植 1.67 DPR/（DPR = Diplomacy Ribbon 扩展/Hide Stats Evolved，DiplomacyRibbon_TPT.lua 2007 行完整独立 fork + WorldRankings 两文件 + 能见度三档参数/热键/文本）+ 并入观察者 mod Better Spectator Mod（BSM，工坊 1916397407）的丝带观察者逻辑（用户需求：移植 DPR、与 BSM 观察者兼容、样式以 DPR 为准；目录 `InGame/DiplomacyRibbonExt/`）——①分组重构（用户裁决）：DiplomacyRibbon.xml 的 StatStack 平铺 15+ Label 改 4 组容器（Group_Stock 组合1 储量 Military/Science/Culture/Gold/Faith/Favor、Group_Rate 组合2 产出 Cities/Food_Total/Production_Total/GoldPerTurn/FaithperTurn/FavorperTurn、Group_TechCivis 科文仪表 6 控件、Group_Observer BSM 观察者组内含 Eras/Army/Yield/Total 4 视图子组 + SpecTag/SpecData/SpecControl_1..6），FinishAddingLeader 约 40 行逐 Label SetHide → 组级 1 次 SetHide；组内个别 SetHide 仅保留特例（观察者 Army 视图隐组内 5 行、能力/胜利门槛 MPT_ApplyCapabilityGates）；子控件一律不带 Hidden="1"（父组隐藏即整组不可见，子控件自身标记会致组显示后仍不可见——XML 初始化坑）②BSM 并入：三键名生态协议原样保留（UIEvents.UIDoObserverPlayer 经 ExposedMembers.LuaEvents 桥发 BSM gameplay 脚本切 POV、GameConfiguration "OBSERVER_ID_<n>" 记录跟随目标、LuaEvents.DiplomacyRibbon_Click 广播刷新——BSM worldtracker 等靠它联动，勿改名）；观察者检测修复（原 BSM 循环误用本地玩家 LeaderType 判断第 i 位，非观察者视角恒不置位、其四象限排序分支成死代码，改查 PlayerConfigurations[i]）；UpdateLeaders 四象限排序（组队×观察者矩阵，普通局保持 DPR 相遇回合排序）；六视图 SpecControl_1..6（Score/Techs/Eras/Army/Yield/Total 单选、再点取消回轮播）+ 20 秒轮播 OnTimePasses（GameCoreEventPublishComplete 驱动 b_hide/b_hide_2 相位翻转）；非观战局 Group_Observer+Logo 整组隐藏零侵入（原 HideSpecInfo 40 余行逐控件 → 2 次 SetHide）；观察者条目仅 Observer 标识、主观察者条目承载 SpecControl（绑定须在 bIsSpec 早退之前，否则按钮永不出现）；徽记 Logo 仅观察者局非观察者条目；Host/Ping 行原受 1.67 MPH_ON 门控改观察者常显；累计四行 GameSummary REPLAYDATASET 求和（组不可见不算）；顺手修复 BSM 三笔误（YieldTypes.Food 大小写→FOOD、sEras/resourceText 全局泄漏→local、Game:GetLocalPlayer() 冒号笔误）③科文仪表真实预估（用户裁决）：RefreshTechMeter/RefreshCivisMeter 的 ScienceBoostMeter/CultureBoostMeter 加速值由 DPR 朴素百分比公式改 MPT_EngineBoost（条目14 引擎精确定点链，Ghidra 反编译 39 组实测全对；include("TechAndCivicSupport") 复用）+ DPR 按目标玩家 GetExtraBoostFromModifiers（条目14 自带版仅本地玩家不可用），Estimates=min(progress+boostValue, cost) 结构保留，MPT_GetBoostValue 兜底回退原公式④裁剪：BSM Data0..Data7 死控件（其 Lua 无条件 SetHide(true)）剔除、Data8→SpecTag、Data→SpecData、BSM Cities（城市数+人口）与 DPR Cities（人口总量）ID 冲突改名 Spec_Cities、BSM 队名前缀不移植（Name 行 [NEWLINE] 队名与 DPR 玩家名测宽居中布局冲突，样式以 DPR 为准）⑤玩家名/文明名开关收编条目12 MPT_Settings 两行（ParameterId 沿用 1.67 原 key DiplomacyRibbon_PlayerInfo_PlayerName/_CiviName，默认开；MPT_Settings_Toggle + TPT_Settings_Toggle 双通道监听，与 1.67 设置互通）⑥WorldRankings（用户裁决：隐藏其他玩家情报数据、不隐藏胜利进度）——核实 DPR 版实为原版 Base/PartialScreens 与 Exp2 版（WorldRankings_Expansion2.lua，外交/科技胜利进度视图）合并单文件（胜利进度全保留），仅 3 处情报隐藏（g_victoryData.VICTORY_CONQUEST.GetScore 军力评分恒 -1、两处 SecondTiebreakSummary 拼接移除），1.67 注册注释「禁用世界排名」实指其 ReplaceUIScript 直接顶掉官方 include 链的降级语义，本 mod 保持合并版语义不降级；其 XML 与原版 Exp2 版零差异原样拷贝。参数/热键沿用 1.67 原 key（SETTINGS_DIPLOMACYRIBBON_TPT/Domain DPRsettings/HotKey_DPR_TechCivisProgress「;」/HotKey_DPR_Totalyield「'」，同装幂等共享）；Model 三档运行时读 GameConfiguration nil 兜底 0 标准（无条件加载 UI 无 criteria，本 mod 惯例）。注册：DiplomacyRibbon 与 WorldRankings 两上下文各 ReplaceUIScript(100000)+ImportFiles(100010)，FE UpdateDatabase(10) 参数+InputActions、UpdateText(100)，IG InputActions(10)+UpdateText(100) 双注册；压过 1.67 DPR 101/102、120/121 与 BSM 9999（其 worldtracker/CityBanner/gameplay 脚本不受影响）。自检：luaparser 解析通过（DPR 副本 244 chunk）、WorldRankings 剥标注解析通过（引擎方言 :table/:ifunction 标注）、modinfo 与 3 配置 XML 严格解析、文本 SQL 18 tag×2 内存库执行无重复。Version 77。条目24修复×4（前三笔记录于 AGENTS.md/提交信息，此处补记）：①WorldRankings TeamTooltip 悬停卡顿——SetToolTipCallback 悬停期间被引擎频繁反复触发、每次 ResetInstances+逐玩家重建实例，按 teamData 表身份去重（每次 GatherXxxData 新建表自动重建、同表重复回调复用实例栈）②去重后首建落全新控件、当帧 GetSizeX 为布局前默认宽度→测量失真卡片过窄文字溢出且被缓存冻结（原版靠 ResetInstances 回收复用每帧重建收敛），缓存命中时补 EqualizeTeamTooltipWidths 等宽校正③外框高度不随实例数收缩——ResetInstances 隐藏回收的旧实例仍是 CivStack 子控件、栈高只增不减，AutoSize 外框跟随残留，MPT_RelayoutTeamTooltipStack（CalculateSize+ReprocessAnchoring）缓存命中与重建两路径各调一次④丝带属性列全空只剩名字分数（用户截图实证，对照 1.65 DPR 正常卡面）——分组首版四组写成 StatStack 下 `<Container>`：ForgeUI Container 不参与 Stack 布局、不随内容自适应尺寸（StatStack:CalculateSize 算不到组高、组内 Label 不被逐行排列，ActiveLeaderAndStats 黄色高亮 SetSizeVal 取 StatStack:GetSize() 高随之失真），组改嵌套 `<Stack StackGrowth="Bottom" StackPadding="3">`（Stack 直接嵌套 Stack 官方惯用法 DiplomacyActionView/Options/GameSummaries，Options.lua 外层单次 CalculateSize 整树重排先例；BSM 观察者控件原就是 StatStack 直接子级、嵌套化恰还原其原始视觉流），隐藏组/行自动跳过布局、组级 SetHide 语义与 Lua 零改动。条目24修复×5（观察者局用户实测五连）：①Score/Yield 两按钮点击无反应——DPR 隐形热区 TPT_Control_1（60x115、Offset 0,60）覆盖实例 y60-175 压住观察者列前两按钮吃掉点击（Total/Techs/Eras/Army 位置更低正常），观察者局整局隐藏（观察者模式下行显隐已由 MPT_RealizeObserverView 接管、DPR 逐 Label 显隐不生效，热键 ; ' 走 Input 事件不受影响）②非主机条目残留「L: na」乱数行——原 BSM Data 行走 "GAP_<playerID>" 平均延迟协议（依赖 1.67 MPH 写入配置，未移植必 nil 恒走 " L: na  " 兜底），剔除协议改仅主机标识/真实延迟（Network.GetPingTime≠-1）存在时显示、无数据整行隐藏（嵌套 Stack 隐藏行自动跳过布局）③Eras「古典噎」「普鮠age」乱码——string.len/string.sub 按【字节】截断 UTF-8 中文切在字节中间（「古典共和国」15 字节>8 → sub(1,7) 半个汉字），删政体/时代两处 Lua 截断交引擎 ReduceWidth=61 干净省略号④行间遮挡——原 BSM 三处 Offset="0,10" 在嵌套 Stack 行距（FontNormal12 行高≈16px）内把行压进下一行，Eras 组三处全部移除；战略资源原 2 资源/行+[NEWLINE] 折行在 61px 列宽逐行截断成图标乱码，改 1 资源/行（table.concat [NEWLINE]）且无库存上限且无存量的资源不占行⑤观察者界面文本汉化 13 tag×2 zh/en（六按钮 Score/Yield/Total/Techs/Eras/Army + SpecTag Observer/SpecData Host/Current:/Next:/Land:/Navy:/Air:），XML 默认 String 换 LOC tag + Lua 顶部预加载 SpecStr_* 局部（带参拼接走 ..），Land/Navy/Air/Strategic1/Nukes XML 占位文案清零防英文闪现。自检：luaparser 剥标注解析通过（2724 行）、SQL 内存库 31 tag×2 无重复、XML ET 解析+七处抽查+y-offset 清零。条目24修复第二轮×5（观察者局用户实测）：①观察者全知视角——UpdateStatValues 全部 16 处 Model 披露门槛（标准/团队档按 g_AccessLevel 分级，军力<3/科文<2/金信仰支持<1 等）加 bGodView 局部（bspec_loc==true 一律 Canshow：观察者无外交渠道永远拿不到能见度等级，BSM 全知语义）；人口/食物/生产力三门槛扩 (Model==0 or Model==3)、军力门槛保持 Model==0——为⑤的「默认」档提供语义（默认=标准+军力恒显示）②产出视图「错误的显示」——毛产 Cities_Prod 与 DPR 生产力总量（GetProduction 同 API 同求和）恒等值重复两行，移出产出视图只留毛粮（余粮=净剩余≠毛粮城市毛产出和），产出视图 7 行；Cities_Prod/Cities_Food 补 ToolTip（原缺，用户实测）③陆军视图核弹两武器同行被 61px 列宽截断遮挡（用户实测截图），拆 [NEWLINE] 两行；条目24修复2：数字改放图标右侧（用户裁决「数字放右边」，原 BSM 数量前置与全列 icon 左数字右行式相反）④Eras 视图 Spec_Cities 行 Housing/Citizen 内联图标大于行高与邻行互侵（用户实测），组 StackPadding 3→8 加行距 + SetText 去空格紧凑防截断 ⑤外交能见度设置加第四档「默认」（用户需求，SETTINGS_DIPLOMACYRIBBON_DEFAULT→Lua Model 3）=在标准（推荐）基础上显示所有玩家军事实力，DomainValues SortIndex 1-4 重排，zh/en SQL 33 tag×2。自检：luaparser 剥标注解析通过（2736 行）、bGodView 16 门槛断言、SQL 内存库 33 tag×2 无重复、Config/丝带 XML ET 解析+Domain 四档/padding/tooltip 抽查【Lua 部分由并行会话以 d4520e9 提交，本提交补 XML/Config/SQL/文档】。条目24优化（WorldRankings 情报门控，用户裁决）：他玩家决胜摘要按外交能见度设置（SETTINGS_DIPLOMACYRIBBON_TPT 四档）门控——本地观察者全可见（原版行为）；标准=军力仅队友、科技/文化/信仰全公开；默认（Model 3）=全可见；团队=非队友按聚合最高能见度分级（科技/文化/信仰≥1、军力≥3；DPR RefreshAccessLevel 语义 GetVisibilityOn 自身+存活队友取最高、队友/自己恒4）；公开=全可见；分数与决胜排序恒真实值（胜利进度不隐藏）。原 DPR 无条件隐藏三处全部还原后改摘要串门控：军力 GetScore 恒-1→真实 GetMilitaryStrengthWithoutTreasury（原版 Base 公式，队伍决胜排序恢复真实军力）、两处 SecondTiebreakSummary 拼接还原；MPT_IsIntelVisible 按决胜类型映射类目（CONQUEST→军力/TECHNOLOGY→科技/CULTURE→文化/RELIGIOUS→信仰，缺省=分数恒可见），隐藏=空串走消费端既有 ~= "" 检查自动跳行（概览 details/队伍 tooltip desc 两消费点）；本上下文与 DiplomacyRibbon 互为独立 Lua 状态，Model/能见度聚合体系自建（MPT_INTEL_MODEL 读同 key 四档、MPT_GetIntelAccessLevel）。自检：luaparser 剥标注解析通过（3592 行）、diff 分块归属核验（banner/g_victoryData/门控段/采集/两消费点全为本任务改动）。条目24：领袖头像 Tooltip 换自定义 ToolTipType（用户裁决「将原本的 ToolTip 换成自定义的 ToolTipType，内容参考点击头像后打开的面板」并指定参照 Exp2 Replacements/DiplomacyActionView.xml）——面板原结构取证：默认视图 L413-429（DiplomacyCivHeaderLarge 领袖/文明名两行、FontFlair14+SmallCaps 区头 String={LOC_LOADING_FEATURES_ABILITIES:upper} 原版 tag 免自建、Line 350,0 宽2 分隔线、FeaturesStack）+ IconInfoInstance/TextInfoInstance（L478-495，CircleRim40 圈+32 图标+FontFlair14 标题+DawnText 描述）。实现：DiplomacyRibbon.xml 声明 MPT_LeaderInfoTooltip 模板（TeamTooltip 同款 Controls_Tooltip 九宫格底板 AutoSize）+ MPT_TraitRowInstance 行实例（Rim 圈图标行/顶格能力行合一，RowTextStack 缩进 36px Lua 切换；金色 RowStats=GetPreText 前置科文/造价/战力行）；Lua 结构化 MPT_GetLeaderInfoSections（条件与顺序照搬 Trait2Text：区域→建筑→改良→单位，各表行须有真名与描述，无特色内容的特性回退特性自身名/描述=能力行，按玩家缓存）+ MPT_FillLeaderTooltip（TTManager 控件表+InstanceManager 懒初始化、无本地玩家/未遇见玩家与原 GetToolTipString 同口径、悬停按 playerID|相遇态 去重——引擎悬停期间反复触发回调同款）+ UpdateIcon 包装（BASE_LeaderIcon_UpdateIcon 后 Portrait:SetToolTipType+SetToolTipCallback，类型优先于字符串，ResetInstances 回收属性保留重复设置幂等）；原纯文本链（LeaderIcon:GetToolTipString/GetPlayerAbilityText/Trait2Text）保留不删。顺手修复 GetPreCT 两处 [ENDCOLORE] 笔误（无效颜色 tag）。自检：luaparser 剥标注解析通过（2907 行）、XML ET 解析（ToolTipType 7 控件/行实例 7 控件/Line 在位）、SQL 回落 33 tag×2 无重复。条目24：领袖富 Tooltip 二轮重构（用户裁决「仿照准备房间中的文明和领袖的面板」+ 首悬停尺寸偏小 + 去除额外属性描述）——取证：准备房间面板非 ToolTip 而是原版 AdvancedSetup.xml CivToolTip 实例族（EnhancedToolTip Grid+ScrollPanel+InfoStack）+ PlayerSetupLogic.lua PopulateLeaderTooltip L833-878（HeaderInstance 居中区头 DivHeader+FontFlair18 glow ShellHeader Locale.ToUpper 大写领袖/文明名；领袖能力=IconInstance CircleBacking45+头像45、文明能力=CivIconInstance CircleBacking44+Circle44_Darker/Lighter 叠层+徽记44、特色内容=IconInfoInstance CircleCompass 34,58,80,150+图标38；标题 FontFlair14 ShellHeader SmallCaps+描述 DawnText，TextStack 缩进 55px；收尾 InfoStack:CalculateSize()+ReprocessAnchoring()）。重构：XML 模板改 InfoStack 单栈 + MPT_SectionHeaderInstance/MPT_TraitRowInstance 双实例（三圈行合一按 kind=header|leader|civ|unique 切换；顺序 区头→领袖能力→文明区头→文明能力→特色内容两来源合并扫描），GetPreText 属性行去除（用户裁决，旧纯文本链 GetToolTipString/GetPlayerAbilityText/Trait2Text/GetPreText 保留不删）；首悬停尺寸偏小修复 = 收口移出去重每次回调都跑（首帧新控件文本未布局实测偏小，悬停期间引擎反复触发回调以已布局控件实测自愈，去重仅免行重建）。自检：luaparser 剥标注解析通过（2928 行）、XML ET 断言（BG=EnhancedToolTip/双实例控件齐/RowStats 清零）】
- 大将军时代提示【已实现为条目25：移植 1.67 BER/UnitFlagManager_GreatGeneralEraReminder.lua（BER = Great General Era Reminder，大将军/海军统帅单位旗帜下沿显示所属时代名横幅；目录 `InGame/GreatGeneralEraReminder/`）——表现层与 1.67 不同（用户裁决：弃「复用晋升徽标改造」改「新建控件绑定父控件」，结构按用户指定为「xml+同名lua 对 + UnitFlagManager_ lua」）：①1.67 原方案 = 复用晋升徽标控件（Promotion_Flag 换 ActionPanel_TurnTimerFrame 纹理 + UnitNumPromotions 写时代名 + 非将军时按锚点判定还原），缺陷：关开关后已改造旗帜永不还原（还原分支在 Show_BER 门控内）②本移植改官方蛮族氏族 DLC 同款「实例模板新建控件」——官方先例：BC 的 UnitFlagManager.xml 在 FlagRoot 内加 TribeStatusFlag 子控件、Lua 直接 m_Instance.TribeStatusFlag 访问（实例模板内带 ID 控件自动挂实例表，嵌套子控件同样，无需运行时装配）；引擎墙取证（决定注入手段）：ContextPtr:BuildInstanceForControl(name,tbl,parent) 仅解析本上下文模板（InstanceManager 内部即此 API）、控制句柄不可经 LuaEvents/ExposedMembers 跨上下文传递（全盘零先例）、动态 IM 实例无 ID 不可 LookUpControl 定位 → 模板必须注册进 UnitFlagManager 上下文；不覆盖原版 XML 的唯一注入手段 = AddUserInterfaces Context=UnitFlagManager（官方此 action 仅用过 InGame，扩展用法——若引擎不把追加布局合并注册进目标上下文，EraBanner.lua 全局函数对旗标上下文不可见，调用方 nil 防御静默关闭，回退方案 = 同名覆盖原版 UnitFlagManager.xml）③三文件：EraBanner.xml（MPT_ERA_Pool 池容器 + MPTEraBanner 模板：EraBanner Image 挂 ActionPanel_TurnTimerFrame 110x20 Anchor C,B Offset 0,-6 + EraLabel FontFlair12 白色 Offset 0,1，视觉参数照抄 1.67，[Size_14] 内联改 XML Style 静态声明）+ EraBanner.lua（实例池 MPT_ERA_Acquire/Release 两全局，协议照抄原版官方动态实例模式——原版 AttentionMarkerInstance 即构建后 ChangeParent 挂 FlagRoot、UnitFlag.destroy 时 ReleaseInstanceByParent 按父控件归还，旗标实例回收复用零残留零重复构建）+ UnitFlagManager_MPT.lua（ReplaceUIScript 100000 + ImportFiles 100010 整文件替换，原版无通配 include 钩子，照抄 1.67 BER 防御 include 链 BuilderCharges→BarbarianClansMode→UnitFlagManager 探测 Initialize 即停，LoadOrder 压过 1.67 BER 10001）④覆写 UnitFlag.UpdatePromotions（GG/统帅有 individual → EraLabel:SetText(时代名)+幂等显隐；m_Instance.MPT_Era 三态：nil 未尝试/false 模板不可用降级/table 实例；base 无条件调用——原版晋升徽标零改动，GG 无经验槽其徽标自然隐藏，1.67 还原分支随之消失=顺手修复其关开关不还原 bug）+ UnitFlag.destroy（先 ReleaseInstanceByParent 归还横幅再 base 销毁）⑤开关收编条目12 MPT_Settings 一行（GreatGeneralEraReminder_Show 沿用 1.67 原 key，默认开；LoadScreenClose 面板 ApplyAll 送达初值）；设置文本沿用 1.67 原文案（1.67 仅 zh 且重复注册两遍，补 en_US）。与 1.67 同装：其 BER 上下文被压制未加载，其设置面板 BER 开关失联属预期，开关以本 mod 面板为准。Version 78】【条目25修复：横幅不显示——首版把 EraBanner 对注册到 AddUserInterfaces Context=UnitFlagManager 期望模板并入旗标上下文，实测不生效。根因取证：引擎 InGame.lua 348 行对 Modding.GetUserInterfaces("InGame") 逐项 ContextPtr:LoadNewContext(addin.ContextPath, Controls.AdditionalUserInterfaces, id, isHidden=true)——AddUserInterfaces 的所有 addin 硬编码挂 InGame 的 AdditionalUserInterfaces 容器成为独立隐藏迷你上下文（节点名 = ContextPath 无扩展名末段，Lua.log「Loading InGame UI -」行实证），Context 指向子上下文时 addin 根本不入桶（Lua.log 零 EraBanner 痕迹）；且迷你上下文 = 独立 Lua 状态，模板即使加载也只在自身上下文可 BuildInstanceForControl（控制句柄不可经 LuaEvents/ExposedMembers 跨上下文，全盘零先例）。修复改 FEB 式直连（用户裁决「直接 LookUpControl :ChangeParent」）：EraBanner 对改注册进 InGame（条目12 同桶同机制），xml 弃 Instance 模板改预声明 40 面唯一 ID 横幅池（EraBanner_1..40 内嵌 EraLabel_1..40，20 人房 × 大将军/统帅两类同屏上限，池耗尽静默）；旗标上下文按路径 LookUpControl("/InGame/AdditionalUserInterfaces/EraBanner/MPT_ERA_Pool[/EraBanner_k][/EraLabel_k]") 取代理（原版先例 PartialScreenHooks.lua 64 行按名寻址该容器下追加上下文）→ ChangeParent 挂 FlagRoot → 代理常驻旗标状态直读直写（SetText/SetHide 零跨状态调用）；归还 = SetHide 后 ChangeParent 回池容器（占位表 MPT_ERA_Used 本状态自持，UnitFlag.destroy 时归还，旗标实例回收复用零残留）；池路径懒解析缓存 + 解析失败静默降级。诊断 print 两处（池上下文加载/池路径解析）待实测确认后删。Version 79】【条目25用户裁决：新控件方案废止，回归 1.67 原版方案 + 同批移植 BCT——LookUpControl 直连方案（条目25修复）实测仍完全不生效，两轮「新建控件绑定父控件」实验废止（引擎 addin = InGame.lua 348 行 LoadNewContext 硬编码挂 InGame 的 AdditionalUserInterfaces 成为独立 Lua 状态迷你上下文，模板/控件无法为旗标上下文所用；但 Lua.log 证实 ReplaceUIScript 整文件替换与 include 链本身工作正常——1.67 同装时其 BCT 文件已被链拉入，日志先后出现 BCT 内部链与入口链两条「as base file」打印），实验文件 EraBanner.xml/.lua 删除。回归 1.67 BER 原版「复用晋升徽标控件改造」方案原样：大将军/统帅 Promotion_Flag 换 ActionPanel_TurnTimerFrame 纹理 + UnitNumPromotions 写 [Size_14] 时代名（白字下移 1 像素）+ 非将军按 GetAnchor()=="C,B" 判定还原原版纹理/锚点/尺寸（还原分支在 Show_BER 门控内的原版行为忠实保留不做修正）；同批移植 1.67 BCT/UnitFlagManager_BuilderCharges.lua——工人/军事工程师旗帜徽标显示剩余劳动力 + Events.UnitChargesChanged 即时刷新（Subscribe/Unsubscribe 覆写叠加原版事件订阅，GetUnitFlag 按玩家+单位反查旗帜），ImportFiles 以 VFS 原名 UnitFlagManager_BuilderCharges.lua 登记——入口链首环按名探测命中即叠加，链序 入口→BCT→BC→原版，与 1.67 BER+BCT 同装形态一致（1.67 中 BER 10001 压过 BCT 4000 后经链叠加）；与 1.67 同装双方 ImportFiles 同名文件，本 mod 100010 压过其注册内容一致幂等；开关双通道监听 MPT_Settings_Toggle + TPT_Settings_Toggle（条目24 DPR 先例，1.67 面板 BER 开关经后者继续生效）。Version 80】【条目25规范：目录/命名/订阅规范化（用户观察裁决）——①目录拆回 1.67 源结构两目录：入口 InGame/BER/UnitFlagManager_MPT.lua + 链文件 InGame/BCT/UnitFlagManager_BuilderCharges_MPT.lua，替代临时合并目录 InGame/GreatGeneralEraReminder/（单一目录混装 BER+BCT 两功能名实不符）②文件 _MPT 后缀规范化：UnitFlagManager_BuilderCharges.lua→UnitFlagManager_BuilderCharges_MPT.lua，include 链探测名同步 MPT 化——与 1.67 同名文件（无 _MPT）不同名，同装时链只探 MPT 名不双重叠加 ③开关订阅删 TPT_Settings_Toggle 双通道行（用户观察「2 行订阅」不规范），单通道 MPT_Settings_Toggle（条目12 面板；1.67 面板 BER 开关不再联动）。Version 81】
- 通知栏提醒扩展【已实现为条目26：移植 1.67 NDR/NotificationPanel_DealRemind.lua（NDR = Notification Deal Remind 交易提醒，条目主体基准）+ 工坊 2459772036 Great Person Recruited Notification（Cptpuk/Yokuyin，他人招募伟人通知，并入次功能；目录 `InGame/NotificationDealRemind/` 单目录两功能，以 DealRemind 为基准）——①DealRemind：链式覆写 OnDefaultAddNotification（chunk local BASE 捕获，同条目19 机制），外交类通知 NOTIFICATION_DIPLOMACY_SESSION 自动展开（OnMouseEnterNotification 原版悬停路径露出操作钮，不受开关控制同 1.67 语义）+ 九种请求文本（提出交易/交易请求/外交团/大使馆/联盟/友好宣言/和平/开边/调整交易）命中播提示音 Play_MP_Game_Waiting_For_Player（3 秒冷却防连响）；相对 1.67 差异五项：零全局污染（1.67 泄漏 TPT_BASE_OnDefaultAddNotification/CheckPassText/Cooldown/g_CoolDownTime 四全局）、匹配文本预加载查集合（规约文本预加载 + O(1) 比对替代 1.67 每条通知 9 次 Locale.Lookup 线性比对）、冷却时钟 os.time（墙钟）→UI.GetElapsedTime（单调，条目18/20 同款修正）、GetNotificationEntry 查册 nil 守卫（1.67 原样 notificationEntry.m_Instance 在面板未收下通知即原版 OnDefaultAddNotification 提前返回分支时踩空崩脚，条目19 修复④同款场景）、设置订阅改 local 回调 MPT_Settings_Toggle+TPT_Settings_Toggle 双通道（开关 key 沿用 1.67 原 NotificationPanel_DealRemind 默认开，1.67 同装其面板亦可控制本功能；1.67 具名全局回调 OnTPT_Settings_Toggle 与同上下文 NOC 补丁同名互覆——条目19 头注释警告对象，本文件规避；开关语义同 1.67 仅控提示音）②GreatPerson：链式覆写 RegisterHandlers（原版 NotificationPanel.lua 1854 行 LateInitialize 运行时调用，晚于 1896 行通配注入 → 包裹必然生效）挂 NOTIFICATION_OTHER_PLAYER_RECRUITED_GREATPERSON handler（MakeDefaultHandlers + AddSound ALERT_NEUTRAL + Activate 复用原版 OnClaimGreatPersonActivate 点击跳伟人界面，1347 行原版全局）；LoadGameViewStateDone 后才订阅 UnitAddedToMap（源样——防读档时地图上既有伟人单位逐个触发弹通知）；配套数据原版全无（通知类型/图标/MESSAGE/SUMMARY 文本经 Base+DLC 全树 grep 零命中，仅 LOC_GREAT_PEOPLE_RECRUITED_BY_UNKNOWN 为原版自带）自带三文件等价移植：GreatPersonRecruited_Data.xml（Types KIND_NOTIFICATION + Notifications MID/回合末过期/AutoNotify=False，UpdateDatabase 10）+ GreatPersonRecruited_Icons.xml（ICON_ATLAS_NOTIFICATIONS[8]=CLAIM_GREAT_PERSON 同款伟人图标，UpdateIcons 10）+ 文本 zh/en SQL（tag 沿用源 mod 原名，源 mod 仅 en/de 且自注 google 翻译，中文补全「伟人已被招募」/「{1_recruiter} 招募了 {2_greatPersonType} {3_greatPersonName}」；Summary 占位符保留——通知 Summary 由引擎排版、中文语序需要，lua 运行时构造不适用「纯文本 tag + Lua 拼接」预加载规约场景）；相对源 mod 差异：未相遇玩家名判定补 nil 防御+观察者分支（源 mod 对 Players[GetLocalPlayer()] 直接 GetDiplomacy，观察者 GetLocalPlayer()=-1 踩空崩脚；本 mod 支持 BSM 观察者局，观察者按 BSM 一切可见语义显示真名）+ 观察者不发通知（本地玩家 -1 无通知信箱）+ 增开关（用户裁决，源 mod 无设置体系：MPT_Settings 行 NotificationPanel_GreatPersonRecruited 自造 key 与 NotificationPanel_DealRemind 对称默认开，仅 MPT 单通道订阅——自造 key 1.67 面板无此参数 TPT 通道无意义，不同于 DealRemind 沿用原 key 走双通道）。注册：两 lua 同一 ImportFiles IG_NDR_Import(1010) 通配注入无 LuaContext（同条目19），IG_NDR_Data(10)/IG_NDR_Icons(10)/IG_NDR_Text(100)，文本仅游戏内注册（同条目21/22 消费侧先例）；设置文本并入条目12 SettingsPanel 双语 SQL（LOC_MPT_SETTINGS_NDR_*/GPR_* 各 2 tag）。与 1.67 同装：其 NDR/NOC 与本条目两文件四通配共载（包裹链兼容功能正常但交易提醒双响+双开关入口），与源 mod 2459772036 同装伟人通知双份（UnitAddedToMap 双订阅、AlwaysUnique 不去重），均建议二选一启用（同条目19 共存立场）。自检：ET 严格解析 modinfo/2 XML、SQL 内存库执行 5 文件（MPT_Settings 11 行含 NDR/GPR 默认 1、LocalizedText 无重复）、luaparser 剥标注解析 2 lua（111/159 行）。Version 82】【条目26修复：交易提醒完全不生效（Lua.log 实证每次通知到达 `NotificationPanel_MPT_DealRemind.lua:78: Type check failed: (expected 'table', but got instance of 'NotificationType')`，包裹在查册处中断 → 自动展开与提示音永不执行；vanilla 逻辑在 BASE 内已先执行故通知显示正常）——根因：移植时给 `local notificationEntry` 补的 `:table` 标注被引擎赋值期类型检查拒绝，GetNotificationEntry 返回 hmake 'NotificationType' 引擎实例非纯 Lua 表（与条目21 GetTrade 等 Get* 接口对象同类；1.67 原样此处无标注）；去标注修复=条目21「引擎对象去 :table 标注」同族坑第 2 例 hmake 实例类（教训：移植补标注仅限 number/string/boolean 与有原版先例的 userdata，凡 hmake/IM/Get* 引擎实例赋值一律不加）；附带 m_Instance nil 守卫（原版 754 行三分支：分组非首个/回合阻塞/图标不可显示时 entry 有册无 UI 实例，1.67 原样传 nil 同样撞类型检查）；九文本查集合改懒构建（首次通知到达时构建一次复用，兼防文件加载期文本库未就绪把原文 tag 当键值的静默失配）。条目26规范（用户裁决）：删 `LuaEvents.TPT_Settings_Toggle.Add` 双通道订阅行，单通道 MPT_Settings_Toggle（同条目25规范「2 行订阅不规范」裁决，1.67 面板 DealRemind 开关不再联动属预期）。校验脚本升级：新增「剥标注后残留 `\)\s*:\s*(table|number|string|boolean)` 零命中」断言——函数返回值标注 `):类型` 引擎方言不支持，本轮连带发现 WorldRankings_MPT.lua:142/164 两处返回值标注致整文件解析失败未加载（unexpected symbol near ':'，条目24优化功能全灭），已删；此前剥标注正则 `(\w)\s*:\s*(类型)` 匹配不到 `):类型`（括号前非词字符）致自检漏检；全 mod lua 扫描返回值标注零残留。luaparser 剥标注解析 3 lua 通过。Version 84】【条目26扩展（用户裁决）：伟人招募通知入栈即自动展开——GPR handler.Add 包裹（MakeDefaultHandlers 的 Add=OnDefaultAddNotification 全包裹链执行完创建条目后，查册 GetNotificationEntry + m_Instance nil 守卫 + OnMouseEnterNotification 原版悬停路径，同 DealRemind 展开机制；GP 类型不触发 DealRemind 的外交类型门控无双展开；MPT_BASE_NotificationAdd 幂等守卫防调试热载重复包裹），不加提示音（DealRemind 响铃不引入，保留源 mod AddSound=ALERT_NEUTRAL 到达音）。Version 86】
- 世界议会弃权选项【已实现为条目27：全新功能（1.67 无此功能，工坊 247 mod 全扫 abstain/弃权 零命中），目录 `InGame/WorldCongressAbstain/`——原版投票 UI = XP2 `DLC/Expansion2/UI/Additions/WorldCongressPopup.lua`(2687行)/.xml(621行)，常规会(stage1)/特别会世界议会(stage2)/结果页(stage4) 共用 WorldCongressPopup 上下文，无通配 include 钩子 → 整文件替换（条目15/17/21/22/23/24/25 模式：ReplaceUIScript 100000 LuaContext=WorldCongressPopup 指向 `WorldCongressPopup_MPT.lua` + ImportFiles 100010 登记 lua 与同名覆盖 xml，XML 文件名必须保持原名）——原版行为：常规会每条决议必须选 A/B+目标+票数>0、特别会每提案 votes≠0，`CanMoveToNextPhase` 强制全表态；引擎原生无弃权概念（全游戏目录 grep 零命中）但 **0 票提交合法（= 弃权的等价实现）**：OnAccept 决议仅票数>0 才发 WORLD_CONGRESS_RESOLUTION_VOTE（0 票自然不发）、提案按表全量发 WORLD_CONGRESS_DISCUSSION_VOTE 0 票引擎接受（未受邀应急成员同款路径）、WORLD_CONGRESS_SUBMIT_TURN 只标记「本玩家投票完毕」与票数无关（无票可投原版自动发送实证 L274-284）→ 纯 UI 改动零 OOS 风险。机制：①每张决议卡（ResolutionItem 标题行右侧）与提案卡（ProposalItem 投票控件下方）加「弃权」GridButton（AbstainButton，WC_ButtonControl 文本态切换「弃权/已弃权」）②弃权 = Abstained 标记（决议存根表+A/B 子表三处，提案存本表）+ 票数清零 + choice/target 复位 -1 + `UpdateVotingWidget` 早退锁定步进器（取消弃权经 UpdateAllVotingWidgets 恢复可用）③`CanMoveToNextPhase` 两段校验豁免 Abstained 项（唯一闸门，其余逻辑原样）④确认页 PopulateSummary/PopulateSummaryProposals 弃权项显示「（弃权）」状态、不显示选项/目标/描述 ⑤决议弃权时 UpdateResolutionChoice 复位卡槽与目标下拉、提案弃权时 UpdateProposal 恢复未选中底纹 + 分类标题票数汇总刷新 ⑥弃权与投票互斥由步进器禁用天然保证（无需改 OnVoteResolution/OnVoteProposal，OnAccept 零改动）⑦stage4 补充提案页不加弃权（原版本就有 PassButton 跳过）。开关收编条目12（MPT_Settings 行 WorldCongressAbstain_Show 自造 key——1.67 无原 key 可沿用——默认开，单通道 MPT_Settings_Toggle 同条目25 规约；关闭时清空全部弃权标记+隐藏按钮+恢复步进器 = 完全还原原版必投行为；OnShutdown 注销监听防热重载泄漏）。文本仅 IG 注册（条目21/22 消费侧先例）：LOC_MPT_WC_ABSTAIN/_ON/_TT/_SUMMARY 4 tag zh/en 双语，lua 顶部预加载缓存。1.67 与工坊均无同文件替换先例，LoadOrder 100000 同既有条目。注册：IG_WCAbstain_Replace/IG_WCAbstain_Import/IG_WCAbstain_Text + Files 登记 4 项 + 设置文本并入条目12 SettingsPanel 双语 SQL（LOC_MPT_SETTINGS_WCABSTAIN_* 各 2 tag）。自检：ET 严格解析 modinfo/弃权 XML、SQL 内存库 4 文件 tag 无重复、luaparser 剥标注解析 MPT lua。Version 83】【条目27修复：弃权无效被引擎随机代投 + 点击崩脚（用户首测双问题）——①弃权决议被随机代投：引擎对「未投过票」的决议会在会期结算时随机代投（首版「0 票不发操作=弃权」的推断被实测推翻——随机代投是引擎对缺席投票者的兜底），修复 = OnAccept 为弃权决议显式发一张 0 票 WORLD_CONGRESS_RESOLUTION_VOTE（PARAM_RESOLUTION_OPTION=1 / PARAM_RESOLUTION_SELECTION=0 合法占位，0 票对结果零影响、0 外交支持消耗），把玩家标记为该决议已投票即不触发随机；提案侧原版本就全量发送 0 票操作（未受邀应急成员从不被随机）为该语义实证 ②点击弃权按钮 Runtime Error L86「attempt to index a nil value」：m_kResolutionChoices 为 nil——原版 OnLocalPlayerTurnEnd 把三张会期表置 nil 但只重置提案两个 IM 不重置决议 IM（m_kResolutionItemIM），会期结束后经 OnResumeCongress 重开界面（该路径不经过 SetStage）旧决议卡仍显示可点，点其上任何控件都 nil 索引崩脚（原版步进器 OnVoteResolution 同款陈旧卡陷阱，本条目按钮首测踩中）；双修复 = OnLocalPlayerTurnEnd 补 m_kResolutionItemIM:ResetInstances()（根治陈旧卡，HasChoices 两 IM 归零一致、ResumeCongress 正确走 BetweenTurns）+ 两个弃权切换回调顶部会期数据 nil 守卫（陈旧卡点击静默无效）+ 提案回调 instance nil 防御。语义同步：lua 头注释/modinfo 注释「决议 0 票不发操作」→「显式 0 票标记已投票」。luaparser 剥标注解析通过、与原版 diff hunk 归属核验。Version 87】【条目27移除（用户裁决）：基于引擎无法跳过投票，功能整体移除——二轮实测确认：显式 0 票决议投票操作（条目27修复方案）仍无法阻止引擎随机代投，引擎在会期结算层面对未投票/0票决议强制随机代投兜底，Lua 层无跳过投票的通道（PlayerOperations 仅 RESOLUTION_VOTE/DISCUSSION_VOTE/SUBMIT_TURN 三操作，引擎函数表无 SetVote 类 API，0 票操作被引擎视同未投票）→ 弃权在引擎层不可实现（除非 DLL 级补丁，超出本 mod 范围）。移除清零：InGame/WorldCongressAbstain/ 目录 4 文件（lua/xml/双语文本）、modinfo 三 action（IG_WCAbstain_Replace/Import/Text）+ Files 4 项、MPT_Settings 行 WorldCongressAbstain_Show、设置面板双语 4 tag（LOC_MPT_SETTINGS_WCABSTAIN_*）。本条目沉淀的引擎事实保留记录（可后续沉淀 civ6 技能库）：①引擎对未投票的世界议会决议在会期结算时随机代投（缺席兜底机制，UI 层无从阻止）②原版 OnLocalPlayerTurnEnd 只置 nil 会期数据表不销毁决议卡实例（m_kResolutionItemIM），OnResumeCongress 重开界面旧卡可点、点其上任何控件 nil 索引崩脚（原版步进器同款陈旧卡陷阱）③引擎方言剥标注自检法修正：先剥 --[[ ]] 长注释再剥行注释，顺序颠倒会啃掉长注释开头留孤儿 ]] 假报错。实现细节见 git 提交 bc1a546（首版）/c02977f（修复）。Version 88】
- 更多快捷键【已实现为条目28：移植 1.65 NHK/（More HotKeys 更多快捷键）全模块 + 并入 CHS-PVP（工坊 3037861572 Core/Main 两个 Queries SQL）扩展单位热键，两源去重合并（目录 `InGame/NewHotKeys/`）——四组功能（用户裁决全选）：①单位命令热键 12 绑定 = 引擎双层（InputActions/InputActionDefaultGestures 注册 + UnitOperations/UnitCommands.HotkeyId UPDATE，保留 WHERE HotkeyId IS NULL 不覆盖玩家已有绑定与原版绑定），4 个与 1.65 同 ID 同键（掠夺 Shift+P/升级 Shift+E/晋升 Shift+G/取消 Shift+S，升级含 CHS 超集 ACTIVATE_GREAT_PERSON）+ CHS 扩展 7（破坏/修复道路·建商路 Ctrl+Shift+P、劫掠商路 Ctrl+Shift+R、传送进城/重新基地 Shift+R、编队/解编、组建军团、组建集团军 Ctrl+Shift+F、唤醒）+ FortifyUntilHeal 复用原版 H 键让船只也可驻扎至痊愈；②键位基准（用户裁决）= 以 1.65 为准（本 mod 用户群沿用 1.65 键位习惯，条目16/20/24/25 同款先例），三处两源冲突改键：编队 Shift+E→Ctrl+Shift+E（让位升级）、唤醒 Shift+W→Ctrl+Shift+W（让位随机晋升）、组建军团 Shift+F→Shift+C（让位强制结束回合，Corps 首字母助记）；原版键位零冲突已经 Base+XP1+XP2 InputConfiguration.xml 全量核验（原版不占用任何 Shift/Ctrl+Shift 字母组合，单键 U/K/S/D 空闲，H 即 FortifyUntilHeal 本尊）；③地图钉四键（Shift+Y 发坐标聊天=建钉→SendPinToChat→监听自己 MultiplayerChat 回声自删钉聊天里只留 pin 图标 / Shift+A 建钉 / Shift+D 删钉 / Shift+M 显隐）：MapPin_HotKey 迷你上下文，运行时 CPL_NO_PINS 门控（条目16「禁用地图钉」联动），DMT_MapPinRemoved 广播原样保留；④随机晋升 Shift+W（CanStartCommand PROMOTE 结果表 math.random 选一直接 RequestCommand，先 CANCEL 清当前命令为晋升清路，间谍除外，UI 本地随机无联机同步问题）+ 伟人自动招募 Shift+K（GreatPeoplePointsChanged 订阅重算时间线，「仅自己可招募或自己类别点数最高」即自动 RequestPlayerOperation 招募，LaunchBar 伟人按钮 GPA_Icon 指示灯）；⑤强制结束回合 Shift+F：拆自 1.65 TurnTime_HotKey.lua（条目20 已移植其 ]/[ 加减时与聊天框旁按钮），自动模式 = 开启立即 ACTION_ENDTURN(UserForced) + 本地回合开始高频重复（GameCoreEventPublishComplete tick 节流）+ 回合开始 5 tick 内被系统自动取消结束状态则重试一次 + 再按取消（ACTION_UNREADYTURN）+ 四角黑边 AlphaAnim 挂 /InGame/WorldInput；对接条目12 FEB 右键预留点 LuaEvents.ForcedEndTurn（1.65 同一通道，预留接口就此激活）；⑥城市远程攻击 CityBannerManager_CSA.lua（11 行）：原版 RangedAttack 键触发且选中城市 CanRangeAttack 时进入 CITY_RANGE_ATTACK 模式，ImportFiles 1000 走原版 CityBannerManager_ 通配 include 链（条目18 MPT 1010 之前）；⑦同装让位 criteria 三件套：MPT_NEW_HOTKEYS（MPT 独立开关，用户裁决不与 1.65 NEW_HOTKEYS 共享，高级选项 SortIndex 2904 紧随其 2903）+ NHK_MPT（开关 ∧ 1.65 不在装——1.65 在装时其 NHK 原版 Lua 继续服务，本 mod Lua 消费上下文全部让位，防双建钉/显隐双翻转互相抵消/双重随机晋升/Shift+F 双翻转；单 Criteria 多子元素默认 AND，技能库 criteria-patterns 实证）+ NHK_MPT_MAP（再 ∧ DMT 高级地图钉 mod 不在装，镜像 1.65 Disable_DMT）；键位绑定 SQL 仅 MPT_NEW_HOTKEYS 门控——共享 4 命令的 WHERE IS NULL 声明竞争因键位一致结果无差异；⑧优化：18 动作注册合并单一 InputActions 文件（1.65 分散 4 XML+1 SQL）；CHS SQL 文本列内嵌中文转正 LOC tag + en 补全（1.65 仅中文；Corps/Army/Wake/Teleport/升级/晋升/取消沿用原版现成 tag 不重复登记）；零全局污染（1.65 NHK 四 Lua 泄漏 20 余个全局全 local 化，条目18 同款约定）；剔除 1.65 反作弊搭车代码（NewUnitOperation.lua 70-152 行：LensToggleStack 镜头指纹广播/作弊特征弹窗/KillCheat 恶意 Modding.UpdateSubscription 死循环）与已注释死键位（Recruit/CancelAll/FortifyAll）；惰性 LookUpControl 控件nil 防御（1.65 MapPin 面板未载踩空）+ 挂载幂等守卫（条目5/12 惯例）+ 点数类别表 nil 防御 + CanRangeAttack nil 守卫；ActionId 沿用两源原名（条目24 DPR 先例：与 1.65 同装 InsertOrIgnore 双向幂等收敛）；⑨注册：FE Config(10)+InputActions(10)+Text(100) / IG Gameplay SQL(10, criteria=MPT_NEW_HOTKEYS)+InputActions(10)+Text(100) / 四迷你上下文 AddUserInterfaces(900)+ImportFiles(900)（同条目12/20 机制）/ CSA ImportFiles(1000)；Files 登记 18 文件；⑩校验：modinfo ET+action id 唯一(124)+criteria 引用闭环+Files 闭环(153)、InputActions 18 ActionId 唯一+键位抽查 12 项、SQL 桩表绑定断言（含 HotkeyId 已占用行不被覆盖）、文本 zh/en 22 tag 对称+自定义 tag 引用覆盖、5 lua 剥标注解析（即摘并行会话刚沉淀的两条引擎方言坑：泛型 for 循环变量标注、函数返回值 ) :type 标注，扫描断言零命中——AutoRecruit/MapPin/NewUnitOperation 各命中一处已修】
- 政策卡收益显示【已实现为条目21：移植 1.67 BRS/ 内打包的 Extended Policy Cards（EPC by Aristos；工坊 2266952591 即 EPC 本体，仅 GovernmentScreen.lua/.xml 两文件，描述自认 Depends on Better Report Screen——依赖实为 Infixo BRS 的 RMA 引擎 ExposedMembers.RMA.CalculateModifierEffect，工坊 1312585482 即 BRS v7.0 本体；1.67 v1.34 起「兼并政策卡mod」把 EPC 的 GovernmentScreen 打包进 BRS/ 目录。用户要求仅移植 EPC 部分）——政府/政策卡界面每张政策卡底部黑底半透明收益条显示真实产出 + tooltip 追加完整效果文本。范围裁决（用户问「哪些代码多余」）：1.67 BRS/ 25 文件中 21 个属 BRS 报告界面本体与配套，EPC+RMA 对其零引用（逐一 grep/diff 验证）全部剔除——reportscreen.lua/.xml + 9 个 BRSPage_*.lua（报告界面主框架与 9 页）、RLL 两文件（报告页按钮注入器）、BetterReportScreen_Database.sql（BRS 版本参数+RLLReports 表）、Config 两文件（F8 ToggleReports 热键）、Icons/FontIconsRMI 两文件（15 图标在 RMA/GovernmentScreen 中 0 引用，仅未移植报告页消费）、Text 三文件（GovernmentScreen 40 个 LOC 全为原版自带，RMA 仅 2 个原版通用 tag）；另记录 1.67 modinfo 注册 9 个不存在语言文件的缺陷。实现（目录 InGame/ExtendedPolicyCards/）：①数据层 MPT_RealModifierAnalysis.lua——RMA v7.0 引擎 ~2900 行整携（MPT_ 改名防 1.67 同装 VFS 撞名），空 Context（MPT_RealModifierAnalysis.xml）+ 同名 lua 自动执行（同条目5/12/13 机制，AddUserInterfaces 900），ExposedMembers.RMA 导出 6 函数，RF/GS 运行时探测/事件订阅/缓存零改动；唯一 BRS 硬依赖第 1 行 print 读 GlobalParameters.BRS_VERSION_*（BRS 数据库 SQL 写入、本 mod 不移植）nil 连接崩 chunk（条目18 cfunction 同类坑），删除即完全独立；②表现层 GovernmentScreen_MPT.lua——三方 diff 验证 = 当前 2.0 原版 + ARISTOS 两块补丁无版本漂移（原版无通配 include 钩子、DLC 仅小 overlay 无完整变体，只能整文件覆盖），ReplaceUIScript 100000 + ImportFiles 100010 压过 1.67 EPC_IMPORT_FILES 12001（条目15/17 模式）+ GovernmentScreen.xml 同名覆盖布局（ARISTOS EffectContainer/Effect 控件，同条目16 MapPinListPanel.xml 先例）；isBRSActive 保持 1.67 硬编码 true（EPC 独立版为 Modding.IsModActive 探测 BRS GUID，本 mod RMA 内置必在），调用点加 RMA.CalculateModifierEffect nil 防御（1.67 无防御，RMA 缺席崩 RealizePolicyCard）。无条件常开同 1.67（先例条目14/15/17，无开关无文本无图标无参数）。已知限制：RMA 输出含少量硬编码英文（Unknown effect 等）保持原样。Version 57】【条目21优化：政策卡收益类型扩展 31→89 全覆盖——经 Cache/DebugGameplay.sqlite 逐一枚举，政策卡（含 attach 一层）实际使用 90 种 EffectType 而引擎仅处理 31 种，其余一律红字 "Unknown effect"（12 张伟人点卡、13 张时代兵种生产卡、军事/外交/间谍/城邦交易/Favor 卡全盲）。新增 MPT 文本行通道（最低侵入 2 hook + 1 分区）：①MPT_LineHandlers 表 58 种 EffectType 的静态参数驱动短文本行（产量图标/资源图标 ICON_RESOURCE_*/GPP 图标 ICON_GREAT_PERSON_CLASS_*/[ICON_Favor][ICON_POWER][ICON_STRENGTH] 等，经 Icons_*.xml 核实），对象名/兵种类(PROMOTION_CLASS_*)/时代/能力走原版 LOC tag 自动本地化，自造短语注册功能文本 SQL（ExtendedPolicyCards_zh/en.sql，61 tag×2 语言；含 9 个官方 UnitAbilities.Name 为 NULL 的隐藏能力中文名，三级回退 官方Name→自有tag→ID原文）②hook① CalculateModifierEffect：per-modifier（direct+attached 各一次）查 MPT_GetModifierLine（按 ModifierId 缓存零陈旧风险）收集成行，产量串后 [NEWLINE] 拼接（卡面 Effect 为 auto 高多行 Label 自然分行）③hook② DecodeModifier：命中 MPT 类型的 modifier 跳过原 ApplyEffectAndCalculateImpact（返回空表）不再误标红字 Unknown。顺带性能优化：CalculateModifierEffect 的 GameInfo.XxxModifiers() 全表扫描（政府界面每次打开 59 卡×全表）改 (表,字段,类型) 懒索引 MPT_GetObjectModifierIds（ModifierId/ModifierID 双拼写兼容随之移入）。核实修正：战争疲劳 EffectType 实名 EFFECT_ADJUST_WAR_WEARINESS（4 政策 modifier 方向 -100/-25 减 +20 加）、GPP 参数实值 GREAT_PERSON_CLASS_*、兵种参数 PROMOTION_CLASS_*、旅游折扣参数名 Modifier、资源图标 ICON_RESOURCE_* 非 RES_*。原 31 种处理逻辑与产量计算零改动（保真）。Version 58】【条目21修复：MPT_RealModifierAnalysis.lua:1836 语法错误 <name> expected near 'function'——类型标注位写保留字 function（local pHandler:function）被引擎 Lua 方言解析期拒绝致 RMA 上下文整个未加载（条目18 cfunction/ifunction 运行期类型检查的解析期同族坑，块配平类自检查不出）；去标注修复（pHandler 仅被调用无类型检查需求），连带去除泛型 for 循环变量标注（for _,x:string in ipairs 原版 UI 零先例，数值 for 标注有先例）。两条语法坑已沉淀 civ6 技能库失败案例库第七批 47/48。Version 59】【条目21修复：GovernmentScreen_MPT.lua:1514 EnsureRowContentsFit attempt to index a nil value（Lua.log 实证发生在 OnLoadGameViewStateDone 后界面未打开时）——卡实例数组 m_ActiveCardInstanceArray 在动画帧回调 EnsureRowContentsFit（SwitchTab 的 RowAnim 每帧驱动）触发时存在槽位空洞（实例填充循环若中断即半空）。政策卡链路内三层防御（不动原版布局逻辑）：①RealizePolicyCard 的 RMA 调用 pcall 化——内部异常不再中断实例填充循环，异常打 MPT_GovScreen: 留痕回退原版行为 ②EffectContainer/Effect nil 防御——控件来自 GovernmentScreen.xml 的 ARISTOS 补丁块，XML 同名覆盖未生效/被改时回退原版行为不崩 ③EnsureRowContentsFit/OverlapProperly 实例缺失防御 + MPT_GovScreen: 定位 print（row/slot/arrayCount，实测确认后删）+ OnRowAnimCallback 开头 ContextPtr:IsHidden 守卫（隐藏态动画回调直接跳过）。Version 60】【条目21用户裁决：显示类型 58→20 收敛（新增 MPT_KnownEffects 静默机制）——用户逐条审核后仅保留「明确的资源数量加成/产出加成(金/信/科/文/产/食/外交支持)/伟人点数/生产对象的生产力加成」20 种：时代/单位/建筑/区域/项目/全项目/太空/全单位/时代奇观 9 种对象生产力、战略资源积累、对城邦商路平产、外交支持三型（每回合/每座建筑/决议返还）、每城免费资源(attach)、伟人点数、影响力点、联盟点、建造者次数（后三者供用户进一步裁决）；其余 38 种登记 MPT_KnownEffects 静默化（费用折扣/旅游/击杀战利/劫掠百分比/使者倍率/审判次数/移动/经验/战力/间谍/厌战/电力/开关定性类）——hook② 同样跳过原链不红字，MPT_GetModifierLine 改 type(pHandler)=="function" 判定防表值 false 被调用。显示行一律去除回合描述（/每回合 删除，MPT_PerTurn 废弃）。图标 token 按本 mod IconViewer_Data.sql（游戏 XML 全量收集 4975 个真实图标文本）核对：[ICON_Favor]→[ICON_FAVOR]（表内原始形式），[ICON_RESOURCE_*/GREAT_PERSON_CLASS_*/BUILD_CHARGES] 经核实存在；免费电力按用户要求静默。功能文本 SQL 收敛为 12 短语×2 语言。自检升级：luaparser 剥标注解析通过（条目22 同款方法）。Version 63】【条目21用户裁决·实际计算：显示从静态参数升级为实际数值——①生产族 9 种走新 MPT_ImpactHandlers 产量通道（ApplyEffectAndCalculateImpact 顶部 hook，真实产量数字 +X [生产图标]，DecodeModifier 城市集合自动跨城求和）：城市当前生产对象经 BuildQueue hash 反查四表（GameInfo 按 hash 索引，同 CitySupport 203-206 行）匹配判定（含 UnitReplaces/BuildingReplaces/DistrictReplaces 文明替代、PromotionClass+PrereqTech→EraType 时代、Projects.SpaceRace、奇观 PrereqTech/Civic 时代区间）→ 城市生产力×Amount% ②资源积累=Amount×玩家提取中地块数（Map 全图遍历+IsResourceExtractableAt 引擎判定含被区域/奇观覆盖，MPT_ExtractCache 缓存）③每城免费资源=Amount×城市数 ④每座建筑支持=Favor×建筑数 ⑤对城邦商路=Amount×对城邦商路条数 ⑥伟人点=Amount×至下一位招募回合数（上限剩余缺口；池经 Game.GetGreatPeople():GetTimeline() 取未招募最低 Cost，点数接口 GetPointsTotal/GetPointsPerTurn 同原版 GreatPeoplePopup 800-801 行）——五类登记 MPT_DynamicHandlers 实时计算不进缓存，RefreshBaseData 时失效行/提取缓存；无收益时整行不显示（如无提取地块/无对城邦商路）。顺带修复：MPT_ImpactProduction/CityProductionImpact 的 pMatch:function 保留字标注复发（luaparser 自检当场抓出，条目21修复沉淀方法见效）。Version 64】【条目21修复：战略资源加成无法显示——根因：GovernmentScreen 的 CalculateModifierEffect 调用传 EPC 原版占位硬编码 ePlayerID=0，动态计算类（资源提取/城邦商路/建筑支持/每城资源/伟人点）全按 0 号玩家数据计算，多人下本地玩家通常非 0 号位 → 资源计数 0 整行静默。修复：MPT_GetModifierLine 动态计算统一改用 Game.GetLocalPlayer()（政策卡即本地玩家视角）；动态 handler 调用 pcall 化（单 handler 异常只静默该行不拖垮整卡）；资源 handler 加一次性诊断 print（MPT_RMA: resource line，定位后删）。Version 66】【条目21修复：动态行计算正确却不显示——诊断 print 实证马/铁计数正确（1/2）而卡面无行，MPT_GetModifierLine 上轮重构误将 return sLine 包进 if not bDynamic 缓存块，动态类结果在函数尾被无条件 return nil 丢弃；修复为缓存写与返回值分离（动态类跳过缓存但正常返回）。Version 67】【条目21修复：POLICY_MILITARY_FIRST 卡 Type check failed: expected 'table', got 'userdata'（MPT_GetGameInfoName 1593 行）——上轮生产族迁 MPT_ImpactHandlers 时漏删 LineHandlers 旧静态行 9 种：①旧 handler 经 GameInfo[sTable] 动态键访问返回 userdata，撞 :table 标注运行时类型检查崩（pcall 防御按设计接住仅该卡回退原版）②更严重：hook② 因 LineHandlers 命中而短路实际计算通道——生产族一直显示旧静态参数文案，实际数值从未生效。修复：删除 9 个旧 LineHandlers 注册（LineHandlers 收敛 19→10），MPT_GetGameInfoName 的 tTable/row 去 :table 标注（动态键访问返回 userdata），自有代码 4 处引擎接口对象（GetTrade/GetGreatPeoplePoints/GetGreatPeople/GetResources）同样去标注（Players[id]/GameInfo 行对象有原版先例保留标注）。教训：迁移式重构须 grep 验证旧注册清零（同名双表注册极易漏删且无任何报错）。Version 65】【条目21用户裁决：卡面 Effect 不换行 + 伟人点数静态化——①卡面/tooltip 分离：CalculateModifierEffect 第 1 返回值改为卡面单行串（MPT 多行收益以 LOC_MPT_EPC_SEPARATOR（zh「，」/en ", "）串接），新增第 5 返回值为 [NEWLINE] 分行版；GovernmentScreen 卡面 Effect:SetText 用第 1 返回值，Draggable/Effect 两处 SetToolTipString 用第 5 返回值（pcall 多值捕获，无第 5 值时回退同串）②伟人点数回退静态贡献点数显示（+n [GPP class 图标]）——原「总计 = Amount × 至下一位招募回合数（上限剩余缺口）」在卡为唯一点数来源时恒等于距下一位还差的点数，数字与卡强度无关（+1/+2 卡显示相同数字）；池计算 API（GetTimeline 未招募最低 Cost / GetPointsTotal/PerTurn(class Index)）经原版 GreatPeoplePopup 696-801 行核实无误，弃用纯因显示语义，整体删除，DynamicHandlers 收敛 5→4（GPP 回归缓存静态类），LOC_MPT_EPC_TO_NEXT tag 废弃删除、新增 LOC_MPT_EPC_SEPARATOR③资源 handler 诊断 print 显示正常实证后删除。自检：luaparser 剥标注解析两文件通过/块配平 1636 token/Line 10+Dynamic 4+Known 39/SQL 13 tag×2 与 Lua 引用零缺失。Version 68】【条目21用户裁决续：卡面串接接缝省略逗号——接缝前段以 [ICON_..] 结尾时图标即天然分隔、仅补空格（新 MPT_JoinCardSeg 判定 %[ICON_..%]$ 结尾），否则才用 LOC_MPT_EPC_SEPARATOR；产量串与文本行间同规则，tooltip [NEWLINE] 版不变。pattern 样本 7 例（icon 结尾/文字结尾/尾括号陷阱）语义验证全过。Version 69】【条目21用户裁决：Req 主体数计算 + 伟人点图标修正 + 三类显示删除——①行数值 = Amount × 合法主体数：MPT_GetModifierLine 加第 3 参 nSubjects（hook① direct/attached 分别传 DecodeModifier 第 6 返回值 = RMA BuildCollectionOfSubjects 按 SubjectRequirementSetId 逐主体 CheckAllRequirements 过滤后的主体集数，COLLECTION_OWNER 恒为 1 玩家主体）；每城免费资源（EFFECT_GRANT_FREE_RESOURCE_EXTRACTED）由「×全部城市数」改为「×合法城市数」——航天承包商（POLICY_FUTURE_VICTORY_SCIENCE→FUTURE_VICTORY_SCIENCE_SPACEPORT_ALUMINUM attach CITY_HAS_SPACEPORT_REQUIREMENTS 城市，子 modifier EFFECT_GRANT_FREE_RESOURCE_EXTRACTED）实测 7 城无宇航中心也显示 +21 即旧算法；伟人点「每座艺术博物馆」行同法 ×博物馆城市数（0 座整行不显示），GPP 重登记动态类（主体数随局势变化，DynamicHandlers 4→5）②伟人点图标改用 GreatPersonClasses.IconString（[ICON_GreatArtist] 等短形式文本内联 token，原版政策描述同款，Vanilla_zh_Hans_CN.xml 26929 行实证）——ICON_GREAT_PERSON_CLASS_* 仅 UI 图集注册（Icons_GreatPeople.xml）无字体字形，文本内联渲染为原文（卡面截图实证）③影响力点/联盟点/建造者次数三类显示删除（handler 改 MPT_KnownEffects 静默不红字：LineHandlers 10→7、Known 39→42、SQL 13→10 短语）。自检：luaparser 剥标注解析通过/块配平 1641 token/Line 7+Dynamic 5+Known 42/SQL 10 tag×2 内存库执行（编辑曾引入 RESOLUTION 重复行，计数自检抓出修正）。Version 70】【条目21用户裁决：资源行格式对齐原版产量串——icon 前数字后紧贴（核实原版 GetYieldString = GetYieldTextIcon..toPlusMinusString 紧贴无空格，YieldTableGetInfo 段间用空格，卡面实测「🌾+4 ⚙+2」即此格式），资源积累/每城免费资源两 handler 行改 [ICON_RES]+N；MPT_JoinCardSeg 新增规则①后段以 [ICON_ 开头（icon 前置收益族行）→ 空格接（先于前段 icon 结尾判定），产量串+资源行/资源行间/伟人点行间均空格、资源行+伟人点行/文字行接缝仍逗号——接缝组合 7 例模拟验证全过。Version 71】【条目21用户裁决：决议返还显示删除 + 同类加成合并——①EFFECT_ADJUST_PLAYER_FAVOR_REFUND_FOR_SUCCESSFUL_RESOLUTION（+50% 世界议会外交支持返还）静默化（LineHandlers 7→6、Known 42→43、SQL 删 LOC_MPT_EPC_RESOLUTION 10→9 短语；MPT_Pct 失去最后消费者随之删除）②同 icon 同布局纯图标数值行合并：handler 第二返回值 tMerge {sIcon, sLayout="first"|"last", nAmount}（伟人点 last、资源积累/每城免费资源 first、每回合外交支持 last；带文字后缀的商路/每座建筑行不参与合并），MPT_GetModifierLine pcall 捕获第二返回值并随行缓存（缓存值改 {sLine,tMerge} 表、无行哨兵改 false），CalculateModifierEffect 收集改结构化条目 {sLine,tMerge}、串接前按 icon|layout 累加合并（保持首现位置）——交响曲「基础 +4」+「每座广播中心 +4×n」→ 一条 +[4+4n]；合并后行尾为图标，接缝按既有图标规则走空格。lua 等价模拟验证：交响曲 +8、骑士阶级不同 icon 不合并、混合串接正确。Version 72】【条目21用户裁决：忠诚度加成显示删除——EFFECT_ADJUST_CITY_IDENTITY_PER_TURN（城市每回合忠诚度）原走原版引擎 LOYALTY 伪产量通道（ApplyEffectAndCalculateImpact → tImpact.LOYALTY → 卡面 [ICON_PressureUp]+N），登记 MPT_KnownEffects 静默（hook② 跳过原链不产出、行通道返回 nil 不红字；Known 43→44）。Version 73】【条目21效率审查：代码效率审查后实施两项（其余项按用户裁决不做——原 RMA tooltip 丢弃功涉及核心签名违反保真原则、GPP 小额重复开销可忽略）——①对城邦商路/每座建筑支持两个动态 handler 的全城遍历加缓存：MPT_GetCSTradeCount（[ePlayerID] 对城邦出站商路条数）与 MPT_GetBuildingCityCount（[ePlayerID][BuildingType] 拥有该建筑城市数，二级惰性建表），同 MPT_GetExtractionCache 机制按玩家缓存、RefreshBaseData 失效（MPT_CSTradeCache/MPT_FavorCache）②hook② 零产量路径共享表 MPT_ZeroImpact 懒初始化替代每主体 YieldTableNew 分配（静默类型+大城市集合时每卡渲染数十个即弃空表；下游 YieldTableAdd 读第二参写第一参已核实、共享零表安全）。luaparser 剥标注解析通过/块配平 1672 token。Version 74】

- 伟人界面增强【已实现为条目29：移植 1.65 BGP/UI/（BGP = Better Great People，Infixo 原作 + 1.65 号码菌「修改背景」补丁，目录 `InGame/BetterGreatPeople/`，描述性命名对齐 InGame 规范）——伟人招募界面（原版 Popups/GreatPeoplePopup）：①招募竞争列表每行显示每回合点数增速 AmountPerTurn（联机抢伟人关键信息）②底部延伸背景 WoodPaneling2/3（号码菌改背景：主背景 768→520 + 底部两段 214x200 平铺补齐，消除高分辨率下背景下缘突兀截断）③往期招募列表倒序（最新在最上）④招募进度区加高（RecruitProgressBox 152→242、RecruitScroll 48→180、RecruitInfo/ButtonStack 下移）+ 效果区收缩（EffectStackScroller 240→158）⑤个人名颜色包裹（名字 tag + `_COLOR` 后缀文本 tag 探测，原版无该 tag 走 GreatPeopleCS 兜底与原版同色——为 mod 彩色伟人名兼容预留）。
  实现：整文件替换原版（条目15/17/21-25 模式：ReplaceUIScript 100000 LuaContext=GreatPeoplePopup + ImportFiles 100010，XML 同名覆盖文件名必须保持原名），无 criteria——运行时开关收编条目12（MPT_Settings 行 BetterGreatPeople_Show 自造 key 默认开，单通道 MPT_Settings_Toggle，面板开着时即时 Refresh 重建内容）。**基底 = 当前原版文件保留引擎类型标注**（1.65 版基于旧版游戏已剥标注，与原版 diff 约 90% 为剥标注非功能改动不采纳，逐 hunk 归属核验仅打 5 处功能补丁）；取证发现原版 GreatPeopleLargeText 样式本就是 FontFlair14+SmallCaps20+ColorSet=GreatPeopleCS——1.65 对 IndividualName 的「样式替换」实际等价于去 ColorSet 让内联 [COLOR] 接管（原版玩家名走兜底同色），故 XML 不改样式仅 Lua 加颜色包裹（开关门控）。
  开关可逆设计：**XML 纯插入不改原版行**（WoodPaneling2/3、AmountPerTurn 默认 Hidden="1"），全部布局值由 Lua 按开关双向套用——顶层控件（背景高度 520/768、延伸背景显隐）MPT_BGP_ApplyLayout 在 Initialize 与开关切换时执行；实例控件（EffectStackScroller 等位于 PanelInstance 模板内，上下文初始化期无法触达）MPT_BGP_ApplyInstanceLayout 在 AddRecruit 建实例时执行；往期倒序 = XML 保持 StackGrowth Down + ViewPast 反序遍历（1.65 用 StackGrowth Up 不可运行时还原，反序遍历视觉等价）；SetOffsetVal/SetSizeY API 经原版 UI 代码核实存在（SetOffset 无先例，SetOffsetVal 才是正名）。
  有意不移植 1 项（文件头横幅留痕）：1.65 注释掉 PeopleScroller 显式水平滚动条（依赖 AutoScrollBar 自动生成替代）——不可运行时还原的纯装饰改动，引擎对「显式滚动条存在但隐藏时 AutoScrollBar 是否补位」行为未验证，保留原版显式滚动条。已知装饰残留：巴比伦英雄模式页签 ResizeHeroPaneling 只同步主背景宽度（原版函数），延伸背景宽度不刷新。
  兼容核验：原版尾部通配 include("GreatPeoplePopup_", true) 保留，XP1/XP2/巴比伦三个覆盖文件（GreatPeoplePopup_Expansion1 重定义 GetPatronize*TT / Expansion2 重定义 IsReadOnly / Babylon_Heroes 重定义 AddCustomTabs/ResetGreatPeopleInstances/LateInitialize）所依赖函数全部原样存在零冲突。
  注册：IG_BGP_Replace/IG_BGP_Import + Files 登记 2 项 + 设置文本并入条目12 SettingsPanel 双语 SQL（LOC_MPT_SETTINGS_BGP_NAME/TT）。自检：XML ET 断言（新控件在位 + 原版行零改动 diff 核验）、luaparser 剥标注解析、modinfo ET 严格解析。Version 89】【条目29修订：去开关恒启用（用户裁决：默认增强即可，不收编条目12）——删 MPT_Settings 行 BetterGreatPeople_Show + 设置面板双语 2 tag（LOC_MPT_SETTINGS_BGP_*），Lua 删 m_bBGPEnabled 总开关变量/OnMPT_BGP_Settings_Toggle 处理函数/MPT_Settings_Toggle 订阅，MPT_BGP_ApplyLayout/MPT_BGP_ApplyInstanceLayout 去分支恒套 BGP 值（背景显形/实例布局/ViewPast 反序/颜色包裹/AmountPerTurn 显隐全部无条件化），modinfo 注释同步；XML 未动（新控件仍默认 Hidden 由 Lua 显形，保留纯插入零改原版行设计）。Version 90】

# 流程与规范
- 按顺序依次实现，每完成一条内容立即停止，等待用户确认功能是否实现

例如完成"房间20人数量上限修改"时立即停止并询问用户是否已经实现

- 对实现功能有疑惑时使用技能/skill:deep-probe 来追问用户
- 代码修改留痕迹与注释，例如
```Lua
-- ============================================================================
-- 修改房间最大人数 12 -> 20
-- g_currentMaxPlayers = math.min(MapConfiguration.GetMaxMajorPlayers(), 12);
g_currentMaxPlayers = math.min(MapConfiguration.GetMaxMajorPlayers(), 20);
-- ----------------------------------------------------------------------------
```

```Xml
<!-- ============================================================================ -->
<!-- 修改本局玩家名称上限 22 -> 128 -->
<!-- 
<EditBox ID="LANPlayerNameEdit" Anchor="L,C" Offset="0,0" Size="parent,23" EditMode="1" Style="FontNormal14" ColorSet="BodyTextCool" FontStyle="stroke" MaxLength="22"/> 
-->

<EditBox ID="LANPlayerNameEdit" Anchor="L,C" Offset="0,0" Size="parent,23" EditMode="1" Style="FontNormal14" ColorSet="BodyTextCool" FontStyle="stroke" MaxLength="128"/>
<!-- ---------------------------------------------------------------------------- -->
```

- 对于新的功能性函数需要添加注释和用法说明。
- 所有文本更改（本地化）一律采用 SQL 格式（INSERT OR REPLACE INTO LocalizedText），通用文本统一存放于 FrontEnd/Text/ 文件夹，每个语言一个文件，同语言集中，使用多行 VALUES；功能专属文本随功能文件夹存放（如更新公告 FrontEnd/Changelog/，数据表与文本同目录）
- 本 mod 新增文本在 StagingRoom.lua 中使用时，按条目分区头后集中预加载为 local XxxStr = Locale.Lookup("LOC_...") 缓存，运行时直接引用变量、不内联 Locale.Lookup；带参数文本不用 Locale.Lookup(tag, args)，改为无参数纯文本 tag + Lua .. 拼接（如 4.1 明细行拆 PREFIX/SUFFIX）。缓存块放分区头之后、本分区函数定义之前。XML String=/ToolTip= 属性与数据驱动的动态 key（row.TextTag、adEntry.ToolTipTag、Modding Name/Description 等）不预加载。
- 同一类函数放在规范的区域
- git管理，每完成一次修改任务后提交git
- 代码要可读性，易管理，命名规范，可扩展性
- 遵循游戏官方的代码风格
- 查看 /skill:civ6 这是专门用于文明6mod开发的技能
- 所有文本均使用.sql，同种语言写在同一个Values下
- modinfo规范
	- Mod id GUID 前8位为0
	- 包含<Name><Description><Teaser><Authors><SpecialThanks><AffectsSavedGames><CompatibleVersions> 并且使用本地化文本多语言，预留<LocalizedText>标签，并在最后完成mod时写入。Description中要多使用\[newline]更加美观
	- modinfo种要包含注释，简要概括该文件的修改内容和作用
	


	
	